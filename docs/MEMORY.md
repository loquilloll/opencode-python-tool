# MEMORY

## [PLANS]
- 2026-02-27T20:08Z [USER] [plan:41-tmux-session-cleanup] Requested cleanup of tmux sessions after launcher troubleshooting.
- 2026-02-27T20:05Z [USER] [plan:40-launcher-cwd-render-fix] Reported tmux blank screen persisted after reattach; requested continued live fix for `~/.local/bin/opencode-fork` startup behavior.
- 2026-02-27T19:43Z [USER] [plan:39-tmux-blank-screen-live-debug] Reported blank screen after attaching to tmux session; requested live remediation while fork remains tmux-hosted.
- 2026-02-27T19:35Z [USER] [plan:38-tmux-fork-session-attach] Requested starting a tmux session pane with `opencode-fork` running and attach instructions.
- 2026-02-27T19:23Z [USER] [plan:37-opencode-fork-black-screen-startup] Reported `~/.local/bin/opencode-fork` opens to a black screen on startup; requested tmux-cli validation and explicit `--port 0` usage during testing.
- 2026-02-27T17:01Z [USER] [plan:36-tmux-ui-load-validation] Requested tmux-cli validation for `opencode-fork` UI load behavior after cwd-parity launcher change.
- 2026-02-27T16:49Z [USER] [plan:35-opencode-fork-cwd-parity] Reported `opencode-fork` defaulting cwd to fork source directory; requested parity with normal `opencode` behavior (start in invoking directory).
- 2026-02-27T14:44Z [USER] [plan:34-test-opf-alias] Requested validation of newly added `opf` alias in `~/.aliases.sh` for running `opencode-fork` with Python tool enabled.
- 2026-02-27T14:32Z [USER] [plan:33-local-fork-install-and-python-tool-sync] Requested local fork usage setup: install/run forked `opencode` for day-to-day driving and update the global custom Python tool.
- 2026-02-27T14:02Z [USER] [plan:32-finish-testing-fix-issues] Requested finishing testing and fixing any issues before user performs personal pre-PR validation.
- 2026-02-27T13:40Z [USER] [plan:31-update-tui-plan-progress] Requested updating the TUI Python permission plan with current implementation progress.
- 2026-02-27T13:36Z [USER] [plan:30-delegated-python-permission-implementation-sync] Directed root orchestration/delegation sync: record completed implementation evidence across root + forked `opencode`, then commit only `docs/CONTINUITY.md` in the root repo.
- 2026-02-27T13:35Z [USER] [plan:29-opencode-python-permission-review-followup] Requested code-review follow-up in forked `./opencode` to render Python `source` in permission body, tighten Python external-directory metadata detection beyond mode-only, add focused helper tests, run focused Bun tests, and commit as a new commit (no amend).
- 2026-02-27T13:32Z [USER] [plan:28-opencode-permission-validation-rerun] Requested dependency restore and focused test rerun in forked `./opencode` to validate new Python permission implementation without product-code edits.
- 2026-02-27T13:29Z [USER] [plan:27-opencode-python-permission-rendering] Requested implementation completion in forked `./opencode` for Python permission helper + renderer + config schema key, add focused helper tests, validate from `packages/opencode`, and commit with provided message.
- 2026-02-27T13:15Z [USER] [plan:26-tui-pr-plan-review] Requested fresh-eyes review of TUI PR plan and test matrix with improvement suggestions, then apply all approved improvements to docs.
- 2026-02-27T12:48Z [USER] [plan:25-opencode-tui-pr-plan] Requested comprehensive implementation planning in markdown for Python permission fullscreen behavior in forked `./opencode`, with commit-safe phases and explicit parallel execution strategy.
- 2026-02-26T18:07Z [USER] [plan:24-readme-install-docs-update] Requested updating README installation documentation.
- 2026-02-26T18:01Z [USER] [plan:23-move-source-to-src] Requested two actions: commit README changes, then move all tool source code into top-level `src/` while keeping the project operational.
- 2026-02-26T15:42Z [USER] [plan:22-comprehensive-readme] Requested rewriting `README.md` into a comprehensive operator guide covering architecture, install/setup, security rationale, runtime behavior, testing, limitations, and upgrade workflow; requested concise continuity updates with the same plan tag.
- 2026-02-26T15:38Z [USER] [plan:21-readme-deny-rationale] Requested README coverage explaining why default Python deny statements are recommended and what security risks exist if those denies are removed.
- 2026-02-26T13:55Z [USER] [plan:20-phase16-routing-subprocess-hardfail] Requested end-to-end Phase 16 implementation across runtime/tool wording/tests plus required validation and docs state updates, while leaving unrelated `.opencode/package.json` and `.opencode/bun.lock` modifications untouched.
- 2026-02-26T13:44Z [USER] [plan:19-phase15-permission-code-visibility] Requested end-to-end implementation and validation of Phase 15: permission prompt code preview metadata in runtime/tests, then docs state updates.
- 2026-02-26T13:40Z [USER] [plan:20-phase16-routing-subprocess-hardfail] Requested plan updates for two features: (1) stronger python-tool description language that reroutes Bash Python heredoc intent to this tool, and (2) immediate failure when subprocess is invoked.
- 2026-02-26T13:32Z [USER] [plan:19-phase15-permission-code-visibility] Requested a new plan phase to add permission-prompt visibility of the code that will be executed by the Python tool.
- 2026-02-26T13:24Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Completed Phase 14 implementation scope: flow-aware tracked-instance handling, import-aware bare-constructor gating, constructor-variant consistency hardening, analyzer regression tests, focused/full Bun validation, and docs status updates.
- 2026-02-26T13:20Z [USER] [plan:18-phase14-atlassian-precision-hardening] Requested addressing code-review findings by first updating the phased plan before additional implementation.
- 2026-02-26T13:05Z [USER] [plan:17-phase13-atlassian-phase-plan] Requested end-to-end Phase 13 delivery: implement Atlassian analyzer coverage, expand analyzer tests, run focused/full Bun validation, and mark plan/continuity status complete.
- 2026-02-26T13:00Z [USER] [plan:17-phase13-atlassian-phase-plan] Requested adding a new plan phase to include known `atlassian-python-api` (`atlassian`) callables in analyzer coverage and tests.
- 2026-02-25T20:57Z [USER] [plan:16-phase12-ghapi-coverage] Implement Phase 12 only from `docs/python-custom-tool-plan.md`: add GHAPI known call-surface coverage (`exec` + workflow `write`) and a bounded `GhApi` instance-variable heuristic in `.opencode/tool/python-analyze.ts`, expand `.opencode/test/python-analyze.test.ts`, validate with focused and full Bun tests, and stop after Phase 12.
- 2026-02-25T20:39Z [USER] [plan:15-phase11-oci-coverage] Implement Phase 11 only from `docs/python-custom-tool-plan.md`: add OCI analyzer classification for known entrypoints, chained client method prefixes, and `oci.config.from_file` read-path handling; update analyzer tests and validate with `.opencode` bun tests; do not implement Phase 12 (GHAPI).
- 2026-02-25T19:21Z [USER] [plan:14-phase10-docs-continuity] Implement Phase 10 only from `docs/python-custom-tool-plan.md`: finalize documentation/continuity state through completed Phase 9 work, mark Phase 10 done, keep Phases 11-12 TODO, run validation checks, and stop before implementation of future phases.
- 2026-02-25T18:08Z [USER] [plan:13-phase9-policy-hardening] Implement Phase 9 only from `docs/python-custom-tool-plan.md`: harden `.opencode/opencode.jsonc` python policy with dangerous deserialization deny rules, preserve existing restrictive defaults (`python:*` ask, dangerous exec denies), keep `unknown:*` and `external_directory` ask posture, validate config compatibility, and stop before Phase 10+.
- 2026-02-25T17:29Z [USER] [plan:12-phase8-runtime-tests] Implement Phase 8 only from `docs/python-custom-tool-plan.md`: add comprehensive runtime tests in `.opencode/test/python.test.ts` reusing `.opencode/test/fixtures/context.ts`, allow runtime file edits only if strictly needed for deterministic tests, and stop before Phase 9+.
- 2026-02-25T17:05Z [USER] [plan:11-phase7-analyzer-tests] Implement Phase 7 only from `docs/python-custom-tool-plan.md`: add fixture context helper and expand `.opencode/test/python-analyze.test.ts` to comprehensive analyzer coverage, then validate with `bun test test/python-analyze.test.ts` (and `bun test` if practical), without starting Phase 8+.
- 2026-02-25T16:43Z [USER] [plan:10-phase6-runtime-hardening] Implement Phase 6 only from `docs/python-custom-tool-plan.md` in `.opencode/tool/python.ts` and `.opencode/tool/python.txt`, run minimal Phase 6 validation checks, and stop before Phase 7+ work.
- 2026-02-25T16:38Z [USER] [plan:09-ghapi-phase-plan] Requested adding a new plan phase to include known `ghapi` module calls (source: `https://ghapi.fast.ai/`) in analyzer coverage and tests.
- 2026-02-25T16:33Z [USER] [plan:08-oci-phase-plan] Requested adding a new plan phase to include known `oci` module calls in analyzer coverage and tests.
- 2026-02-25T16:30Z [USER] [plan:07-update-plan-callable-fallback] Update `docs/python-custom-tool-plan.md` so phased documentation reflects generic callable fallback classification and permission-pattern behavior.
- 2026-02-25T16:25Z [USER] [plan:06-callable-unknown-fallback] Implement generic callable-based fallback classification in `.opencode/tool/python-analyze.ts` so unrecognized calls emit callable-identity events and are permission-checkable without relying on `unknown:no-classified-call`.
- 2026-02-25T15:41Z [USER] [plan:01-cwd-bootstrap] Keep development in repository root, initialize git in cwd, and ignore `./opencode` reference folder.
- 2026-02-25T15:41Z [ASSUMPTION] [plan:02-python-tool] Implement full no-fork custom `python` tool under `./.opencode/tool` with AST-based permission classification, external path checks, timeout/abort handling, and restrictive default permissions.

## [DECISIONS]
- 2026-02-27T20:05Z [CODE] [plan:40-launcher-cwd-render-fix] Update launcher execution path from direct Bun source invocation to package-context invocation (`bun run --cwd <packages/opencode> --conditions=browser ./src/index.ts`) and inject caller directory as positional project only when no positional arg is present.
- 2026-02-27T19:23Z [CODE] [plan:37-opencode-fork-black-screen-startup] Keep `--port 0` in launch-time troubleshooting, and correct the configured default model to a currently available model ID (`openai/gpt-5.2-codex`) instead of relying on removed `-high` suffix variants.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] Replaced unbounded `codeFull` metadata design with bounded `codeExpanded` (only when truncated + ≤ 50 KB) plus `codeExpandedAvailable` flag; `AskInput.metadata` has zero SDK-level size validation so all size discipline must come from the tool.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] TUI render precedence now separates compact view (`codePreview` always) from fullscreen view (`codeExpanded` -> `input().code` -> `codePreview`), reflecting that `info()` accesses two distinct data sources (`request.metadata` vs `input()`).
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] External_directory asks from Python will get a dedicated sub-case rendering Python context (mode/description/source) alongside directory info, detected via `metadata.mode` presence.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] Phase 0 (branch setup) folded into Phase 1 preamble to reduce process overhead.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] `python` will be registered as a named permission key in config schema for discoverability (alongside `bash`, `task`); runtime already works via `.catchall()`.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] Renderer validation strategy: helper is unit-tested in isolation; renderer is validated via tmux E2E captures only (no existing `permission.tsx` test patterns to follow).
- 2026-02-25T20:57Z [CODE] [plan:16-phase12-ghapi-coverage] Keep GHAPI handling bounded to explicit call sets plus simple assignment heuristic (`<identifier> = GhApi(...)` / `ghapi.*.GhApi(...)`); do not introduce full alias/scope symbol resolution in this phase.

## [PROGRESS]
- 2026-03-04T18:15Z [TOOL] Synced local opencode dev by stashing changes, rebasing onto origin/dev (715b844), and restoring local edits; no conflicts.
- 2026-02-27T20:08Z [TOOL] [plan:41-tmux-session-cleanup] Listed active tmux sessions (`remote-cli-session` with 8 windows), executed `tmux kill-server`, and verified cleanup with `tmux ls` -> `no server running on /tmp/tmux-1000/default`.
- 2026-02-27T20:05Z [TOOL] [plan:40-launcher-cwd-render-fix] Reproduced rendering split in pseudo-terminal traces: direct source launch (`bun --conditions=browser .../src/index.ts`) stopped after capability handshake, while package-context launch (`bun run --cwd .../packages/opencode --conditions=browser ./src/index.ts`) rendered full TUI.
- 2026-02-27T20:05Z [TOOL] [plan:40-launcher-cwd-render-fix] Patched `~/.local/bin/opencode-fork` to run through package cwd and preserve caller project semantics for option-only invocations by appending caller dir when no positional arg is detected.
- 2026-02-27T20:05Z [TOOL] [plan:40-launcher-cwd-render-fix] Validated patched launcher with `script` and tmux (`remote-cli-session:7`): full OpenCode UI now renders under `~/.local/bin/opencode-fork --port 0`.
- 2026-02-27T19:43Z [TOOL] [plan:39-tmux-blank-screen-live-debug] Verified tmux state during report: window `remote-cli-session:2` active with fork process alive (`pgrep -af .../src/index.ts`), but `tmux-cli capture --pane=remote-cli-session:2` returned empty output.
- 2026-02-27T19:43Z [TOOL] [plan:39-tmux-blank-screen-live-debug] Dev log for startup (`~/.local/share/opencode/log/dev.log`) showed normal bootstrap/event connection and no session/message lifecycle entries after injected keystrokes, indicating UI loop not progressing to interactive session handling.
- 2026-02-27T19:43Z [TOOL] [plan:39-tmux-blank-screen-live-debug] Performed live restart while client remained attached (`tmux list-clients` showed `attached,focused`), force-killed stale fork PID, and relaunched pane command as `TERM=tmux-256color COLORTERM=truecolor ~/.local/bin/opencode-fork --port 0`.
- 2026-02-27T19:43Z [TOOL] [plan:39-tmux-blank-screen-live-debug] Verified fresh process PID and environment from `/proc/<pid>/environ`: `TERM=tmux-256color`, `COLORTERM=truecolor`.
- 2026-02-27T19:35Z [TOOL] [plan:38-tmux-fork-session-attach] Launched tmux window `remote-cli-session:2` via `tmux-cli launch "zsh"`, then started fork TUI from repo root with `~/.local/bin/opencode-fork --port 0` using `tmux-cli send`.
- 2026-02-27T19:35Z [TOOL] [plan:38-tmux-fork-session-attach] Verified active fork process with `pgrep -af "opencode/packages/opencode/src/index.ts"` showing Bun command including `--port 0`.
- 2026-02-27T19:23Z [TOOL] [plan:37-opencode-fork-black-screen-startup] Reproduced startup/runtime behavior with explicit port flag: `~/.local/bin/opencode-fork --port 0 run "say hi in one word"` failed before fix with `ProviderModelNotFoundError` for `openai/gpt-5.2-codex-high`.
- 2026-02-27T19:23Z [TOOL] [plan:37-opencode-fork-black-screen-startup] Validated model availability via `~/.local/bin/opencode-fork models`; list includes `openai/gpt-5.2-codex` and excludes `openai/gpt-5.2-codex-high`.
- 2026-02-27T19:23Z [TOOL] [plan:37-opencode-fork-black-screen-startup] Updated `~/.config/opencode/opencode.jsonc` default `model` to `openai/gpt-5.2-codex`; post-change checks succeed in both direct shell and tmux (`tmux-cli execute "~/.local/bin/opencode-fork --port 0 run 'say hi in one word'" --pane=remote-cli-session:1` => `Hi`).
- 2026-02-27T17:01Z [TOOL] [plan:36-tmux-ui-load-validation] Verified in tmux-cli remote mode that both `opencode-fork` and stock `opencode` show identical startup traces under `timeout` (`config` load logs then long-running interactive process, exit code `124` from timeout), indicating no fork-specific early crash.
- 2026-02-27T17:01Z [TOOL] [plan:36-tmux-ui-load-validation] Confirmed `opf` alias now resolves correctly in tmux after user escaped `\$schema`; `tmux-cli execute "source ~/.aliases.sh; opf debug config | jq -r '.tools.python'"` returns `true`.
- 2026-02-27T17:01Z [TOOL] [plan:36-tmux-ui-load-validation] While `opf` was launched in a tmux pane, separate pane process inspection showed active fork runtime (`bun --conditions=browser .../opencode/packages/opencode/src/index.ts`), then process terminated cleanly via `tmux-cli interrupt`.
- 2026-02-27T16:49Z [CODE] [plan:35-opencode-fork-cwd-parity] Updated launcher `~/.local/bin/opencode-fork` to remove `bun run --cwd ...` and execute absolute entrypoint directly: `bun --conditions=browser $REPO_ROOT/opencode/packages/opencode/src/index.ts "$@"`.
- 2026-02-27T16:49Z [TOOL] [plan:35-opencode-fork-cwd-parity] Verified from `/tmp` with `opencode-fork --print-logs --log-level DEBUG debug config`; runtime logs now show `service=default directory=/tmp` and `service=project directory=/tmp`.
- 2026-02-27T14:47Z [TOOL] [plan:34-test-opf-alias] Validated corrected alias form `OPENCODE_CONFIG_CONTENT="{\"\$schema\":...}"` by executing `opf debug config | jq -r '.tools.python'` in a non-interactive shell with alias expansion enabled; result `true`.
- 2026-02-27T14:44Z [TOOL] [plan:34-test-opf-alias] Sourced `~/.aliases.sh` in a non-interactive shell with alias expansion enabled; `opf --version` executes and returns `local`.
- 2026-02-27T14:44Z [TOOL] [plan:34-test-opf-alias] `opf debug config` fails with `Configuration is invalid at OPENCODE_CONFIG_CONTENT` due malformed JSON payload from alias quoting.
- 2026-02-27T14:32Z [TOOL] [plan:33-local-fork-install-and-python-tool-sync] Confirmed Bun runtime now satisfies fork build requirement (`bun --version` => `1.3.10`), then installed side-by-side launcher at `~/.local/bin/opencode-fork` targeting `$REPO_ROOT/opencode/packages/opencode/src/index.ts`.
- 2026-02-27T14:32Z [TOOL] [plan:33-local-fork-install-and-python-tool-sync] Updated global custom Python runtime at `~/.config/opencode/tools/python/python.ts` with bounded `codeExpanded`/`codeExpandedAvailable` metadata behavior and synced `~/.config/opencode/tools/python/python.txt` prompt text.
- 2026-02-27T14:32Z [TOOL] [plan:33-local-fork-install-and-python-tool-sync] Added missing parser dependency in global config workspace (`cd ~/.config/opencode && bun add tree-sitter-python@0.25.0`) and validated tool module load (`python tool loads ok`).
- 2026-02-27T14:02Z [TOOL] [plan:32-finish-testing-fix-issues] Re-ran root `.opencode` validation: `bun test test/python.test.ts` => `37 pass, 0 fail`; `bun test` => `74 pass, 0 fail`.
- 2026-02-27T14:02Z [TOOL] [plan:32-finish-testing-fix-issues] Re-ran fork `packages/opencode` validation: focused suites remained green (`69 pass, 0 fail`) and full suite now passes (`1180 pass, 5 skip, 0 fail`).
- 2026-02-27T14:02Z [CODE] [plan:32-finish-testing-fix-issues] Fixed failing unrelated full-suite test `test/pty/pty-output-isolation.test.ts` by hardening PTY subscriber identity in `opencode/packages/opencode/src/pty/index.ts` to bind both `ws.data` and `ws.send` snapshot references, preventing output leak when socket metadata mutates in-place.
- 2026-02-27T13:40Z [TOOL] [plan:31-update-tui-plan-progress] Updated `docs/opencode-tui-python-permission-pr-plan.md` with current status: Phase 1-3 marked done with commit/test evidence (`484f0bd`, `bdc1d13`, `9f1bb60`, focused `69 pass, 0 fail`), and Phase 4-5 explicitly marked TODO.
- 2026-02-27T13:40Z [TOOL] [plan:31-update-tui-plan-progress] Updated `docs/opencode-tui-python-permission-test-matrix.md` progress snapshot and checklists to reflect completed A1/B1/B2 work and pending C1/D1 execution.
- 2026-02-27T13:36Z [TOOL] [plan:30-delegated-python-permission-implementation-sync] Verified root commit `484f0bd` (`feat: add bounded expanded-source metadata for python permission prompts`) implementing bounded `codeExpanded` permission metadata; associated root targeted tests were previously recorded as passing.
- 2026-02-27T13:36Z [TOOL] [plan:30-delegated-python-permission-implementation-sync] Verified forked `opencode` implementation commits `bdc1d13` (initial Python permission summary/fullscreen rendering) and `9f1bb60` (follow-up source-context rendering fix).
- 2026-02-27T13:36Z [TOOL] [plan:30-delegated-python-permission-implementation-sync] Confirmed post-install validation outcome in `./opencode`: final focused run `bun test test/permission-python.test.ts test/config/config.test.ts` => `69 pass, 0 fail`.
- 2026-02-27T13:35Z [TOOL] [plan:29-opencode-python-permission-review-followup] Updated `opencode/packages/opencode/src/cli/cmd/tui/routes/session/permission.tsx` Python permission body rendering to include `Source: <metadata.source>` so Python external_directory prompts surface source/mode/description context together.
- 2026-02-27T13:35Z [TOOL] [plan:29-opencode-python-permission-review-followup] Tightened `isPythonExternalDirectoryMetadata` in `opencode/packages/opencode/src/cli/cmd/tui/routes/session/permission-python.ts` to require `mode` plus at least one Python-specific signal (`source` or `codePreview` or `codePreviewTruncated`) instead of mode-only detection.
- 2026-02-27T13:35Z [TOOL] [plan:29-opencode-python-permission-review-followup] Expanded focused helper tests in `opencode/packages/opencode/test/permission-python.test.ts` for source extraction (`view.source`) and negative mode-only metadata classification behavior.
- 2026-02-27T13:35Z [TOOL] [plan:29-opencode-python-permission-review-followup] Validation passed from `opencode/packages/opencode`: `bun test test/permission-python.test.ts test/config/config.test.ts` => `69 pass, 0 fail`.
- 2026-02-27T13:35Z [TOOL] [plan:29-opencode-python-permission-review-followup] Committed follow-up changes in `opencode` as `9f1bb60` with message `fix(tui): include python source context in permission metadata view`.
- 2026-02-27T13:32Z [TOOL] [plan:28-opencode-permission-validation-rerun] Restored monorepo dependencies in `opencode` using `bun install --frozen-lockfile`; install completed successfully with 3864 packages.
- 2026-02-27T13:32Z [TOOL] [plan:28-opencode-permission-validation-rerun] Re-ran focused tests from `opencode/packages/opencode`: `bun test test/permission-python.test.ts test/config/config.test.ts` => `67 pass, 0 fail`.
- 2026-02-27T13:29Z [TOOL] [plan:27-opencode-python-permission-rendering] Added focused helper tests at `opencode/packages/opencode/test/permission-python.test.ts` covering compact/fullscreen precedence (`codePreview`, `codeExpanded`, `input.code`), summary field extraction (`description`, `mode`, `scriptPath`, `args`, `workdir`), and Python external_directory metadata detection via `mode`.
- 2026-02-27T13:29Z [TOOL] [plan:27-opencode-python-permission-rendering] Updated `opencode/packages/opencode/test/config/config.test.ts` key-order test fixture/assertion to include explicit `permission.python` entry while preserving order/catchall behavior.
- 2026-02-27T13:29Z [TOOL] [plan:27-opencode-python-permission-rendering] Committed forked opencode changes as `bdc1d13` with message `feat(tui): render python permission summary and fullscreen code view`.
- 2026-02-27T13:15Z [TOOL] [plan:26-tui-pr-plan-review] Updated `docs/opencode-tui-python-permission-pr-plan.md` with 8 improvements: bounded `codeExpanded` design replacing `codeFull`, dual-source render precedence (compact vs fullscreen), external_directory Python sub-case in Phase 3, config schema registration in Phase 2, test strategy acknowledgement, Phase 0→1 merge, upstream rebase timing before Phase 3, expanded acceptance criteria.
- 2026-02-27T13:15Z [TOOL] [plan:26-tui-pr-plan-review] Updated `docs/opencode-tui-python-permission-test-matrix.md` with: `codeExpanded`/`codeExpandedAvailable` field names, expanded A1/B1/B2 checklists, 5 new scenarios (S5-S9: args, workdir, description, very large script, external_directory Python), expanded exit criteria.
- 2026-02-27T12:48Z [TOOL] [plan:25-opencode-tui-pr-plan] Added `docs/opencode-tui-python-permission-pr-plan.md` with full objective/scope, dual-repo architecture, commit-safe phased implementation plan (tool metadata + TUI renderer + tmux QA), acceptance criteria, risks, and PR strategy.
- 2026-02-27T12:48Z [TOOL] [plan:25-opencode-tui-pr-plan] Added `docs/opencode-tui-python-permission-test-matrix.md` with lane-based parallel work matrix, per-phase checklists, isolated `tmux-cli` runbook (explicitly avoiding `~/.config/opencode`), scenario matrix, and exit criteria.
- 2026-02-26T18:07Z [TOOL] [plan:24-readme-install-docs-update] Updated `README.md` install section with clearer source/destination variable-driven copy commands, explicit `src/` + `.opencode/tool` wrapper requirements, post-install smoke check, dependency merge guidance, and test-inclusive install validation commands.
- 2026-02-26T18:01Z [TOOL] [plan:23-move-source-to-src] Committed comprehensive README as `c5cf972` (`docs: add comprehensive project README`).
- 2026-02-26T18:01Z [TOOL] [plan:23-move-source-to-src] Moved canonical tool source from `.opencode/tool/` to `src/` (`src/python.ts`, `src/python-analyze.ts`, `src/python.txt`) and added `src/env.d.ts`; converted `.opencode/tool/python.ts` and `.opencode/tool/python-analyze.ts` into thin re-export wrappers targeting `src/`.
- 2026-02-26T18:01Z [TOOL] [plan:23-move-source-to-src] Updated `README.md` file map and install/upgrade instructions to reflect `src/` as canonical source plus `.opencode/tool` wrapper entrypoints.
- 2026-02-26T18:01Z [TOOL] [plan:23-move-source-to-src] Validation passed in `.opencode`: `bun test` => `73 pass, 0 fail` after source relocation.
- 2026-02-26T15:42Z [TOOL] [plan:22-comprehensive-readme] Rewrote `README.md` from minimal content into a full operator-focused guide with sections for project overview, architecture/file map, requirements/setup, tool-only and test-inclusive installation into other projects, recommended permission configuration, expanded deny-rule security rationale, runtime behavior (`code` vs `scriptPath`, permission preview metadata, subprocess hard-fail), testing commands, known limitations/tradeoffs, and update/upgrade sync workflow.
- 2026-02-26T15:38Z [TOOL] [plan:21-readme-deny-rationale] Added root `README.md` with a dedicated security section describing recommended default `permission.python` deny rules and the associated risks (dynamic code execution, shell/process orchestration, and unsafe deserialization) when denies are not enforced.
- 2026-02-26T13:55Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Updated `.opencode/tool/python.txt` routing guidance to explicitly redirect Bash heredoc-style Python execution (`python <<'EOF'`) to the `python` tool and clarify that the tool executes Python directly rather than shell orchestration.
- 2026-02-26T13:55Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Added pre-permission runtime guard in `.opencode/tool/python.ts` to reject direct `subprocess.*` usage immediately after analysis and before any `context.ask(...)`, with deterministic reroute guidance to the `bash` tool.
- 2026-02-26T13:55Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Expanded `.opencode/test/python.test.ts` with inline and script-mode subprocess rejection tests that assert immediate failure, zero permission asks, and explicit `bash` reroute messaging; validation passed in `.opencode`: `bun test test/python.test.ts` => `36 pass, 0 fail`; `bun test` => `73 pass, 0 fail`.
- 2026-02-26T13:55Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Updated `docs/python-custom-tool-plan.md` to mark Phase 16 `DONE` in both the phase section and summary table.
- 2026-02-26T13:44Z [TOOL] [plan:19-phase15-permission-code-visibility] Implemented `.opencode/tool/python.ts` permission-preview helper + shared ask metadata payload (mode/scriptPath/preview/truncation/byte+line counters) wired into both `external_directory` and `python` asks without altering execution/permission-plan semantics.
- 2026-02-26T13:44Z [TOOL] [plan:19-phase15-permission-code-visibility] Expanded `.opencode/test/python.test.ts` with Phase 15 runtime assertions for inline and script preview metadata, deterministic truncation marker behavior for large source, and metadata parity on external-directory asks.
- 2026-02-26T13:44Z [TOOL] [plan:19-phase15-permission-code-visibility] Validation passed in `.opencode`: `bun test test/python.test.ts` => `34 pass, 0 fail`; `bun test` => `71 pass, 0 fail`.
- 2026-02-26T13:44Z [TOOL] [plan:19-phase15-permission-code-visibility] Updated docs state: `docs/python-custom-tool-plan.md` marks Phase 15 `DONE` in section and summary table.
- 2026-02-26T13:40Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Updated `docs/python-custom-tool-plan.md` with new Phase 16 (Python Tool Routing + Subprocess Hard-Fail), including tool-description routing language updates, pre-permission subprocess rejection behavior, runtime test scope, verification commands, summary-table row, and commit-sequence entry.
- 2026-02-26T13:32Z [TOOL] [plan:19-phase15-permission-code-visibility] Updated `docs/python-custom-tool-plan.md` with new Phase 15 (Permission Prompt Code Visibility), defining runtime metadata changes to include executable code preview in permission asks, targeted runtime tests, verification commands, summary-table row, and commit-sequence entry.
- 2026-02-26T13:24Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Updated `.opencode/tool/python-analyze.ts` to replace flow-insensitive Atlassian assignment pre-pass with source-order timeline processing for `assignment` + `call` nodes, including reassignment invalidation and import-provenance-gated bare constructor classification while preserving existing OCI/GHAPI and callable-fallback behavior.
- 2026-02-26T13:24Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Expanded `.opencode/test/python-analyze.test.ts` with Phase 14 regressions for call-before-assignment, reassignment invalidation, bare-name collision protection, and module-qualified constructor variant matrix across Jira/Confluence/Bitbucket/ServiceDesk.
- 2026-02-26T13:24Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Validation passed in `.opencode`: `bun test test/python-analyze.test.ts` => `37 pass, 0 fail`; `bun test` => `67 pass, 0 fail`.
- 2026-02-26T13:24Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Updated `docs/python-custom-tool-plan.md` to mark Phase 14 `DONE` in both the phase section and summary table.
- 2026-02-26T13:20Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Updated `docs/python-custom-tool-plan.md` with new Phase 14 (Atlassian Classification Precision Hardening), covering flow-aware instance tracking, import-aware bare-name gating, constructor-variant consistency, regression tests, and optional runtime permission assertions.
- 2026-02-26T13:05Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Updated `.opencode/tool/python-analyze.ts` with bounded Atlassian coverage: known constructor callables now classify as `exec`, tracked instance assignments map to client families (`jira`, `confluence`, `bitbucket`, `servicedesk`), and known methods normalize to `atlassian.<client>.<method>` exec IDs.
- 2026-02-26T13:05Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Expanded `.opencode/test/python-analyze.test.ts` with Phase 13 coverage for constructor classification, tracked-instance method normalization, unknown-method fallback, and deferred alias behavior.
- 2026-02-26T13:05Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Validation passed in `.opencode`: `bun test test/python-analyze.test.ts` => `33 pass, 0 fail`; `bun test` => `63 pass, 0 fail`.
- 2026-02-26T13:05Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Updated `docs/python-custom-tool-plan.md` to mark Phase 13 as `DONE` in both the phase section and the phase summary table.
- 2026-02-26T13:00Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Updated `docs/python-custom-tool-plan.md` with new Phase 13 (Atlassian Python API Module Call Coverage), including known constructor/method call-surface plan, bounded tracked-instance heuristic, verification scope, summary table row, and commit-sequence entry.
- 2026-02-25T20:58Z [TOOL] [plan:16-phase12-ghapi-coverage] Updated `docs/python-custom-tool-plan.md` to mark Phases 11-12 as `DONE` in both section headers and the phase summary table after implementation/validation.
- 2026-02-25T20:57Z [TOOL] [plan:16-phase12-ghapi-coverage] Updated `.opencode/tool/python-analyze.ts` with `GHAPI_EXEC_CALLS` and `GHAPI_WRITE_CALLS`; added `exec` classification for known GHAPI module/helper/convenience surfaces and `write` classification for `ghapi.actions` workflow helper calls.
- 2026-02-25T20:57Z [TOOL] [plan:16-phase12-ghapi-coverage] Added bounded GHAPI instance heuristic: analyzer now tracks assignments like `api = GhApi(...)` / `ghapi.all.GhApi(...)` / `ghapi.core.GhApi(...)` and classifies `<instance>.<group>.<verb>(...)` calls as normalized `exec` events (`ghapi.<group>.<verb>`).
- 2026-02-25T20:57Z [TOOL] [plan:16-phase12-ghapi-coverage] Expanded `.opencode/test/python-analyze.test.ts` with deterministic Phase 12 coverage for direct GHAPI module calls, `GhApi` convenience methods, workflow helper writes, instance-heuristic normalized calls, and alias fallback unknown behavior.
- 2026-02-25T20:57Z [TOOL] [plan:16-phase12-ghapi-coverage] Validation passed in `.opencode`: `bun test test/python-analyze.test.ts` => `29 pass, 0 fail`; `bun test` => `59 pass, 0 fail`.
- 2026-02-25T20:39Z [TOOL] [plan:15-phase11-oci-coverage] Updated `.opencode/tool/python-analyze.ts` with `OCI_EXEC_CALLS` and `OCI_CLIENT_METHOD_PREFIXES`, added OCI exec classification for known SDK entrypoints and chained constructor->method calls, and added `oci.config.from_file` read classification with first positional or `file_location` argument path extraction.
- 2026-02-25T20:39Z [TOOL] [plan:15-phase11-oci-coverage] Expanded `.opencode/test/python-analyze.test.ts` with deterministic Phase 11 OCI coverage tests for known constructor/pagination exec calls, `oci.config.from_file("~/.oci/config")` literal read path, dynamic `file_location` read, chained client method exec, and alias-form callable fallback unknown behavior.
- 2026-02-25T20:39Z [TOOL] [plan:15-phase11-oci-coverage] Validation passed in `.opencode`: `bun test test/python-analyze.test.ts` => `24 pass, 0 fail`; `bun test` => `54 pass, 0 fail`.
- 2026-02-25T19:21Z [TOOL] [plan:14-phase10-docs-continuity] Updated `docs/python-custom-tool-plan.md` to reflect delivered state: Phases 6-10 statuses are now `DONE` in both section headers and the phase summary table, while Phases 11-12 remain `TODO`.
- 2026-02-25T19:21Z [TOOL] [plan:14-phase10-docs-continuity] Refined Phase 10 known-limitation bullets to match implemented analyzer/runtime behavior, explicitly documenting callable fallback (`unknown:callable:<name>`), alias deferral, dynamic attribute gaps, indirect-call gaps, and inner-`exec` analysis boundary.
- 2026-02-25T18:08Z [TOOL] [plan:13-phase9-policy-hardening] Updated `.opencode/opencode.jsonc` Phase 9 policy with four new deny entries for dangerous deserialization exec patterns: `exec:pickle.load`, `exec:pickle.loads`, `exec:marshal.load`, `exec:marshal.loads`.
- 2026-02-25T18:08Z [TOOL] [plan:13-phase9-policy-hardening] Validation passed in `.opencode`: `bun test` => `50 pass, 0 fail`, confirming workflow parse/load compatibility after policy change.
- 2026-02-25T17:29Z [TOOL] [plan:12-phase8-runtime-tests] Added `.opencode/test/python.test.ts` with 30 deterministic Phase 8 runtime tests covering validation, permission asks/always patterns, external-directory detection, script-path behavior, ENOENT/workdir semantics, process execution, timeout/abort metadata, and metadata streaming/cap behavior.
- 2026-02-25T17:29Z [TOOL] [plan:12-phase8-runtime-tests] Validation passed in `.opencode`: `bun test test/python.test.ts` => `30 pass, 0 fail`; `bun test` => `50 pass, 0 fail`.
- 2026-02-25T17:05Z [TOOL] [plan:11-phase7-analyzer-tests] Added `.opencode/test/fixtures/context.ts` with `createMockContext()` that returns a mock `ToolContext` and captures `asks` / `metadatas` arrays for test assertions.
- 2026-02-25T17:05Z [TOOL] [plan:11-phase7-analyzer-tests] Replaced `.opencode/test/python-analyze.test.ts` with comprehensive Phase 7 analyzer coverage across open/path handling, Path methods, os/shutil writes, json/yaml, exec calls, Phase 5 network+db+tempfile+deserialization, edge/fallbacks, dedupe/mixed calls, and decorator/class/nested/list-comprehension contexts.
- 2026-02-25T17:05Z [TOOL] [plan:11-phase7-analyzer-tests] Validation passed in `.opencode`: `bun test test/python-analyze.test.ts` => `20 pass, 0 fail`; `bun test` => `20 pass, 0 fail`.
- 2026-02-25T16:43Z [TOOL] [plan:10-phase6-runtime-hardening] Updated `.opencode/tool/python.ts` with runtime hardening: venv-aware Python resolution order (`OPENCODE_PYTHON_BIN` -> `$VIRTUAL_ENV/bin/python3` -> `<worktree>/.venv/bin/python3` -> `<worktree>/venv/bin/python3` -> `python3`), script `ENOENT` read error message with resolved path, passthrough env plus `PYTHONDONTWRITEBYTECODE=1` and `PYTHONUNBUFFERED=1`, and non-zero exit code metadata appended inside `<python_metadata>` while preserving timeout/abort metadata.
- 2026-02-25T16:43Z [TOOL] [plan:10-phase6-runtime-hardening] Rewrote `.opencode/tool/python.txt` with expanded guidance for python-vs-bash usage, `code` vs `scriptPath`, non-interactive execution, `args`/`workdir` semantics, output truncation/metadata behavior, permission asks, and security posture notes.
- 2026-02-25T16:43Z [TOOL] [plan:10-phase6-runtime-hardening] Validation passed: module load smoke check `bun -e "import('./tool/python.ts').then(() => console.log('module loads ok'))"` and existing test suite `bun test` (in `.opencode`) reporting `9 pass, 0 fail`.
- 2026-02-25T16:38Z [TOOL] [plan:09-ghapi-phase-plan] Updated `docs/python-custom-tool-plan.md` with new Phase 12 (GHAPI Module Call Coverage), including known `ghapi` call surfaces, optional `GhApi` instance heuristic, test scope, summary table row, and commit-sequence entry.
- 2026-02-25T16:33Z [TOOL] [plan:08-oci-phase-plan] Updated `docs/python-custom-tool-plan.md` with new Phase 11 (OCI Module Call Coverage), including known `oci` call list, classification intent (`exec`/`read` for `oci.config.from_file`), test scope, summary table row, and commit-sequence entry.

## [DISCOVERIES]
- 2026-03-04T18:15Z [TOOL] `gh repo sync <user>/opencode -b dev --source <upstream>/opencode` failed with HTTP 404 on `merge-upstream`; `gh repo view` reports `isFork: true` but `parent: null`, so the sync API cannot be used until the parent relationship is exposed.
- 2026-02-27T20:05Z [CODE] [plan:40-launcher-cwd-render-fix] In this environment, launching local source via direct Bun entrypoint can stall post-terminal-capability handshake (blank screen), while invoking the same entrypoint through package cwd (`bun run --cwd ... ./src/index.ts`) renders normally.
- 2026-02-27T20:05Z [CODE] [plan:40-launcher-cwd-render-fix] Caller-cwd parity can be preserved with package-cwd launch by appending caller directory as the positional project argument only for option-only invocations (for example `--port 0`).
- 2026-02-27T19:43Z [ASSUMPTION] [plan:39-tmux-blank-screen-live-debug] UNCONFIRMED: launching a fullscreen OpenTUI app in a tmux window before a client is attached may leave terminal-capability negotiation in a non-rendering state; restarting after attach appears safer.
- 2026-02-27T19:43Z [CODE] [plan:39-tmux-blank-screen-live-debug] tmux pane shells in this environment inherit `TERM=xterm-256color`; explicit launch with `TERM=tmux-256color` + `COLORTERM=truecolor` is now applied for fork TUI startup to reduce terminal-mode mismatch risk.
- 2026-02-27T19:23Z [CODE] [plan:37-opencode-fork-black-screen-startup] The configured default model in `~/.config/opencode/opencode.jsonc` (`openai/gpt-5.2-codex-high`) is invalid in the current provider catalog; requests fail with `ProviderModelNotFoundError` until overridden or corrected.
- 2026-02-27T19:23Z [TOOL] [plan:37-opencode-fork-black-screen-startup] tmux-cli confirms the same behavior path as local shell: default model fails immediately, while `--model openai/gpt-5.2-codex` succeeds, which isolates startup symptom to model-resolution config rather than fork launcher runtime.
- 2026-02-27T17:01Z [TOOL] [plan:36-tmux-ui-load-validation] tmux-cli `capture` in remote non-attached mode does not reliably render alternate-screen TUIs; absence of drawn UI in capture is not evidence of launch failure when runtime process remains active.
- 2026-02-27T16:49Z [CODE] [plan:35-opencode-fork-cwd-parity] `bun run --cwd <fork-path>` in wrapper scripts forces OpenCode instance/project directory detection to the fork path; launching Bun with the absolute entrypoint and no `--cwd` preserves caller cwd semantics.
- 2026-02-27T14:44Z [CODE] [plan:34-test-opf-alias] In alias value `OPENCODE_CONFIG_CONTENT="{\"$schema\":...}"`, shell expands `$schema` to empty, producing invalid JSON key; escaping as `\$schema` or using single-quoted JSON avoids this.
- 2026-02-27T14:32Z [CODE] [plan:33-local-fork-install-and-python-tool-sync] Running fork from source as `opencode-fork` cleanly reuses existing config resolution (`~/.config/opencode/opencode.json{,c}` and project `.opencode` layers) because config loading is path-based (`Global.Path.config`) and independent of packaged install location.
- 2026-02-27T14:02Z [CODE] [plan:32-finish-testing-fix-issues] PTY socket identity based only on `ws.data` is insufficient when runtimes reuse the same websocket object and mutate state in-place; guarding with both `ws.data` and `ws.send` snapshot references eliminates observed cross-session output leakage in test coverage.
- 2026-02-27T13:36Z [TOOL] [plan:30-delegated-python-permission-implementation-sync] Validation state changed from dependency-blocked to green after `bun install --frozen-lockfile` in `./opencode`; same focused permission/config suite advanced from `67 pass, 0 fail` to `69 pass, 0 fail` after follow-up fix.
- 2026-02-27T13:35Z [CODE] [plan:29-opencode-python-permission-review-followup] Mode-only metadata (`{ mode: "inline" }`) is insufficient to reliably identify Python external_directory asks; requiring an additional Python-specific signal (`source`, `codePreview`, or `codePreviewTruncated`) avoids over-classifying generic external-directory prompts.
- 2026-02-27T13:29Z [TOOL] [plan:27-opencode-python-permission-rendering] Validation in this environment is blocked by missing package resolution in `packages/opencode`: `bun test` fails early with module-not-found errors (for example `xdg-basedir`, `glob`, `remeda`, `@clack/prompts`, `@opencode-ai/util/*`) plus preload failure `@opentui/solid/preload` in subprocess-based tests, so helper/config assertions could not be runtime-verified here.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] `AskInput.metadata` type is `{ [key: string]: any }` with zero SDK-level size validation; metadata is serialized to JSON over IPC and stored in session state, so unbounded payloads risk UI lag, IPC bottlenecks, and memory pressure in the host process.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] TUI `info()` function accesses two distinct data sources: `request.metadata` (from `ask()` call) and `input()` (tool input parameters). The original plan conflated them into a single precedence chain; they serve different roles (compact summary vs fullscreen source).
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] Existing `external_directory` branch in `permission.tsx` expects `metadata.parentDir`/`metadata.filepath` which the Python tool does not provide; Python external_directory asks carry `source`/`mode`/`codePreview` instead, causing degraded display with generic directory-only rendering.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] No tests exist for the `permission.tsx` TUI component (zero matches for `PermissionPrompt` in test files). Helper logic can be unit-tested independently but renderer testing requires either Ink test utilities or tmux E2E captures.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] `python` permission works at runtime via `.catchall(PermissionRule)` in config.ts:638, but is not registered as a named key in the permission config schema (config.ts:618-637). Adding it explicitly improves discoverability and editor autocompletion.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] When `codePreviewTruncated === false`, `codePreview` already IS the full source; any `codeFull`/`codeExpanded` field would be completely redundant in this case.
- 2026-02-27T13:15Z [CODE] [plan:26-tui-pr-plan-review] Plugin tools registered via `ToolRegistry` only get their own explicit `ask()` calls; the outer wrapper `ctx.ask()` at permission.tsx:850 is MCP-only. No double-prompt risk for the Python tool.
- 2026-02-27T12:48Z [CODE] [plan:25-opencode-tui-pr-plan] Existing TUI fullscreen mechanics already exist in `opencode/packages/opencode/src/cli/cmd/tui/routes/session/permission.tsx` (`fullscreen` prompt + `ctrl+f` toggle), so primary gap is python-specific permission rendering and full-source metadata availability rather than new fullscreen infrastructure.
- 2026-02-27T12:48Z [CODE] [plan:25-opencode-tui-pr-plan] Isolated test runs can avoid local global config by setting `OPENCODE_TEST_HOME` + XDG env vars and unsetting `OPENCODE_CONFIG*`; this supports deterministic tmux-based TUI QA without relying on `~/.config/opencode`.
- 2026-02-26T18:07Z [CODE] [plan:24-readme-install-docs-update] Because wrapper entrypoints under `.opencode/tool` now re-export canonical modules from `src/`, install docs must instruct copying both locations together; copying only wrapper files yields broken imports in downstream repos.
- 2026-02-26T18:01Z [CODE] [plan:23-move-source-to-src] Relocating runtime/analyzer to `src/` requires explicit dependency imports that resolve from repository root into `.opencode/node_modules` for Bun-based test/runtime loading; using wrapper entrypoints under `.opencode/tool/` preserves OpenCode integration while centralizing source in `src/`.
- 2026-02-26T15:42Z [CODE] [plan:22-comprehensive-readme] Current runtime behavior to document is: permission asks include bounded source preview metadata (4000-byte/120-line limits + deterministic truncation marker), and direct `subprocess.*` calls hard-fail before any permission ask with explicit reroute guidance to `bash`.
- 2026-02-26T15:38Z [TOOL] [plan:21-readme-deny-rationale] Repository had no existing README file, so fulfilling the request required creating a new root `README.md` and adding an explicit section for deny-rule rationale and consequences of disabling those rules.
- 2026-02-26T13:55Z [CODE] [plan:20-phase16-routing-subprocess-hardfail] A runtime guard on analyzed event identities (`call` starts with `subprocess.`) deterministically blocks direct subprocess orchestration before permission planning/asks, preserving existing permission semantics for all non-subprocess flows and leaving alias-based forms (`import subprocess as sp`) deferred.
- 2026-02-26T13:44Z [CODE] [plan:19-phase15-permission-code-visibility] Permission prompts now expose the exact to-be-executed source in bounded metadata with deterministic truncation marker (`...<python_permission_preview_truncated>`) plus preview/total byte and line counters, and the same preview payload is reused for both permission ask types.
- 2026-02-26T13:40Z [CODE] [plan:20-phase16-routing-subprocess-hardfail] Current python-tool docs encourage python-vs-bash selection but do not explicitly reroute Bash heredoc-style Python execution (`python <<'EOF'`) to the python tool, and runtime does not yet enforce a hard subprocess block before permission asks.
- 2026-02-26T13:32Z [CODE] [plan:19-phase15-permission-code-visibility] Current permission ask metadata in `.opencode/tool/python.ts` includes `source`, `description`, and `operations` but does not include the executable source text itself, so reviewers cannot inspect code directly in the permission prompt.
- 2026-02-26T13:24Z [CODE] [plan:18-phase14-atlassian-precision-hardening] Source-order processing of assignment/call nodes eliminates false normalization before assignment and supports deterministic invalidation after reassignment without introducing alias/scope graph resolution.
- 2026-02-26T13:24Z [CODE] [plan:18-phase14-atlassian-precision-hardening] Bare Atlassian constructor names are collision-prone in user code; gating bare-name exec classification on `import atlassian`/`from atlassian ...` provenance retains bounded precision while keeping module-qualified `atlassian.*` constructors explicit and always classifiable.
- 2026-02-26T13:20Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Code-review findings indicate current Atlassian normalization is flow-insensitive and can over-classify calls across assignment order/reassignment; tightening to order-aware tracking plus import-aware bare constructor gating should improve precision without requiring full alias/scope analysis.
- 2026-02-26T13:05Z [CODE] [plan:17-phase13-atlassian-phase-plan] Existing call-name extraction captures both bare constructors (`Jira`) and dotted forms (`atlassian.confluence.ConfluenceCloud`), enabling explicit constructor exec classification without alias graph expansion.
- 2026-02-26T13:05Z [CODE] [plan:17-phase13-atlassian-phase-plan] A bounded assignment heuristic (`<name> = <known-constructor>(...)`) is sufficient to normalize high-signal instance calls (for example `jira.jql(...)` -> `exec:atlassian.jira.jql`) while unknown methods continue to permission-check via `unknown:callable:<name>`.
- 2026-02-26T13:00Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Official Atlassian Python API docs expose stable client constructor entrypoints (`Jira`, `Confluence`, `ConfluenceCloud/Server`, `Bitbucket`, `Cloud`, `ServiceDesk`) and documented high-signal method families (`jql`, `create_issue`, `create_page`, `open_pull_request`, `create_customer_request`) suitable for explicit analyzer call coverage.
- 2026-02-25T20:57Z [CODE] [plan:16-phase12-ghapi-coverage] Tree-sitter call-name resolution for chained constructor usage already produces dotted forms like `GhApi.create_release`, so convenience helpers can be classified with exact call-set matching while constructor-call unknowns remain unchanged.
- 2026-02-25T20:57Z [CODE] [plan:16-phase12-ghapi-coverage] Bounded assignment tracking is sufficient for module-level GHAPI instance calls (`api.git.get_ref` -> `exec:ghapi.git.get_ref`) without broadening to alias/scope graph analysis; alias imports like `import ghapi.all as ga` still intentionally fall back to `unknown:callable:ga.GhApi`.
- 2026-02-25T20:39Z [CODE] [plan:15-phase11-oci-coverage] Tree-sitter call-name resolution already captures chained constructor->method OCI calls as dotted identities (for example `oci.object_storage.ObjectStorageClient.put_object`), so prefix-based exec classification cleanly upgrades these without alias/scope tracking.
- 2026-02-25T20:39Z [CODE] [plan:15-phase11-oci-coverage] OCI import alias resolution remains deferred by design: `import oci as cloud; cloud.object_storage.ObjectStorageClient(...)` continues to emit callable-fallback unknown events (`unknown:callable:cloud.object_storage.ObjectStorageClient`) and is still permission-checkable.
- 2026-02-25T19:21Z [CODE] [plan:14-phase10-docs-continuity] Plan document drift had persisted after prior implementation phases (6-9 were implemented but marked TODO); this pass reconciles status truth so section-level and summary-table status values now agree.
- 2026-02-25T19:21Z [CODE] [plan:14-phase10-docs-continuity] Keeping Phases 11-12 as TODO is required to preserve roadmap clarity: OCI/GHAPI explicit classification is still pending even though generic callable fallback coverage already exists.
- 2026-02-25T18:08Z [CODE] [plan:13-phase9-policy-hardening] Existing wildcard deny `exec:os.exec*` remains necessary and intact; with last-match-wins permission evaluation and glob-style matching, it continues to cover `exec:os.execvp`/`exec:os.execvpe` variants while new deserialization denies close analyzer-classified gaps.
- 2026-02-25T17:29Z [CODE] [plan:12-phase8-runtime-tests] `MAX_METADATA_LENGTH` trim behavior yields up to `30005` chars in metadata output (`30_000` slice plus `"\n\n..."` suffix and occasional trailing newline from runtime output), so cap assertions should allow `<= 30005`.
- 2026-02-25T17:05Z [CODE] [plan:11-phase7-analyzer-tests] Current analyzer semantics differ from the original Phase 7 examples for some path extraction cases: `Path(path_var).read_text()` emits both `read:Path.read_text` with `dynamicPath:true` and an additional `unknown:callable:Path` event from the constructor call.
- 2026-02-25T17:05Z [CODE] [plan:11-phase7-analyzer-tests] `os.unlink('f')`, `os.rename('a','b')`, and `os.replace('a','b')` currently classify as `write` with `dynamicPath:true` (no literal path extraction), while `os.remove` and `shutil.copy/copytree/move` still resolve literal paths; tests were aligned to this implemented behavior.
- 2026-02-25T16:43Z [CODE] [plan:10-phase6-runtime-hardening] Existing source-empty validation (`!source.trim()`) already allows comment-only source (for example `# note`) to execute, so Phase 6's no-calls/non-blocking behavior needed no extra runtime gate changes.
- 2026-02-25T16:38Z [TOOL] [plan:09-ghapi-phase-plan] Official docs (`https://ghapi.fast.ai/`, `core.html`, `page.html`, `auth.html`, `graphql.html`) show `ghapi` has dynamic endpoint calls through `GhApi` groups (for example `api.git.get_ref`), plus stable helpers (`ghapi.page.paged/pages`, `ghapi.graphql.gh_query`, `ghapi.actions.create_workflow_files`) suitable for explicit analyzer classification.
- 2026-02-25T16:25Z [CODE] [plan:06-callable-unknown-fallback] Existing `unknown:*` permissive allow in `buildPermissionPlan` can be narrowed safely for callable-derived unknown events by tagging analyzer fallback calls with a deterministic `callable:` prefix, while keeping parser/meta unknown markers on `unknown:*`.
- 2026-02-25T15:41Z [TOOL] `tree-sitter-python@0.25.0` package includes `tree-sitter-python.wasm`, compatible with `web-tree-sitter` runtime loading approach.
- 2026-02-25T15:41Z [TOOL] Current workspace root previously had no git repository (`fatal: not a git repository`) until initialization in this session.
- 2026-02-25T15:47Z [TOOL] `web-tree-sitter` root node exposes `hasError` as a boolean property, not a function; parse-failure detection in analyzer must check `tree.rootNode.hasError`.
- 2026-02-25T15:47Z [TOOL] Analyzer smoke checks: `open('data.json','r') -> read`, `open('out.txt','w') -> write`, `subprocess.run(...) -> exec`, malformed source -> `unknown:parse-error`.
- 2026-02-25T16:30Z [CODE] Reference bash tool analysis: bash tool uses tree-sitter-bash for command parsing, arity-based permission prefix system (~130 commands), two-phase permission (external_directory + bash), plugin hook for shell.env, Shell.killTree (SIGTERM→200ms→SIGKILL), output truncation (2000 lines / 50KB) with file persistence. Our python tool mirrors this architecture but uses Python-specific AST classification instead of command arity.
- 2026-02-25T16:30Z [CODE] Permission system uses last-match-wins evaluation with wildcard matching (`*` → `.*`, `?` → `.`). Pattern ending in ` *` (space+wildcard) makes trailing portion optional. Config supports per-tool objects with pattern→action mappings, plus `.catchall(PermissionRule)` for custom tools.
- 2026-02-25T16:30Z [CODE] Known limitation: import alias tracking not implemented. `import subprocess as sp; sp.run()` sees `sp.run` not `subprocess.run`. Deferred — requires scope analysis.
- 2026-02-25T16:18Z [TOOL] [plan:05-analyzer-hardening] `bun test test/python-analyze.test.ts` (in `.opencode`) reports `7 pass, 0 fail`, confirming new classification paths and context-recursive call detection.
- 2026-02-25T16:17Z [TOOL] [plan:05-analyzer-hardening] Alias deferral remains unchanged after Phase 5 checks: `import requests as rq; rq.get(...)` currently results in `unknown:no-classified-call` because alias tracking is intentionally not implemented.
- 2026-03-05T15:07Z [TOOL] Compaction triggered: exceeded upper token threshold; truncated oldest entries to at or below lower target; archived to docs/MEMORY.md.
- 2026-03-05T20:11Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T20:10Z [TOOL] [plan:42-python-tool-output-visibility] `session/index.tsx` routes only `bash` to a dedicated renderer and sends all other tools (including `python`) through `GenericTool`, which is hidden unless `generic_tool_output_visibility` is enabled.
- 2026-03-05T20:11Z [CODE] [plan:42-python-tool-output-visibility] 2026-03-05T20:10Z [CODE] [plan:42-python-tool-output-visibility] `src/python.ts` already streams metadata fields needed for TUI rendering (`output`, `description`, `command`, `cwd`, final `exit`) via `context.metadata`, so a dedicated TUI renderer can be added without changing Python tool protocol.
- 2026-03-05T20:33Z [TOOL] [plan:42-python-tool-output-visibility] Full package run `cd opencode/packages/opencode && bun test` currently fails on pre-existing `test/config/config.test.ts` permission key-order assertion (`expected [*,bash,write,python,external_directory]`, received with `python` first) and timed out; this failure is unrelated to `session/index.tsx` renderer edits.
- 2026-03-05T20:43Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:05Z Read-only Phase 1 review found Python renderer precedence edge case: `meta().output` (even empty string) always overrides `props.output`, which can hide final output when metadata/output diverge in completed state.
- 2026-03-05T20:44Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:05Z Focused read-only Phase 1 review found parity/privacy gap in `session/index.tsx`: Python `workdirDisplay` can reveal cwd/workdir when `sync.data.path.directory` is unavailable, while Bash hides workdir in that case; also found plan doc frontmatter status inconsistency (`status: proposed` vs Phase 1 DONE notes).
- 2026-03-05T20:46Z [CODE] [plan:42-python-tool-output-visibility] Review found follow-up risks in session/index.tsx: Python inline complete state can be truthy by fallback command, metadata.output precedence can mask final output edge-cases, and cwd/workdir visibility differs from Bash when base path context is unavailable.
- 2026-03-05T20:47Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T20:45Z Review found 5 follow-ups: Python pending-state parity issue, output source/type robustness gaps, metadata-vs-final output precedence edge case, Bash/Python workdir parity mismatch when base path missing, and plan frontmatter status drift.
- 2026-03-05T20:51Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:20Z Re-review of `session/index.tsx` confirms prior Phase 1 gaps are addressed: pending inline completion now gated by `ready()`, completed-state output precedence favors final output over metadata stream, and Python workdir hides when base path is unavailable (Bash parity).

## [OUTCOMES]
- 2026-03-04T18:15Z [TOOL] Updated opencode dependencies with `bun install --frozen-lockfile`; repo remains ahead 3 commits with local edits in permission TUI files.
- 2026-03-04T16:51Z [CODE] Restructured python tool sources so `src/python.ts` imports from `src/python/` (python-analyze, python.txt, env.d.ts), added dual-context node_modules resolution in analyzer, and updated global tools layout to `tools/python.ts` + `tools/python/*`.
- 2026-03-04T16:33Z [TOOL] Fixed global python tool import path to use `@opencode-ai/plugin`, copied `env.d.ts` into `~/.config/opencode/tools/python/`, and verified Bun module load succeeds.
- 2026-03-04T16:26Z [TOOL] Updated global OpenCode python tool by syncing `src/python.ts`, `src/python-analyze.ts`, and `src/python.txt` into `~/.config/opencode/tools/python/`.
- 2026-03-03T18:42Z [CODE] Updated `~/.local/bin/opencode-fork` to export `PWD` as the caller directory so relative project paths resolve from launch location.
- 2026-03-03T12:57Z [TOOL] Reinstalled global python tool files from repo and ran `opencode --print-logs --log-level DEBUG run "say hi in one word"`; run completed successfully with output `Hi` and no fatal errors (only cached plugin/duplicate-skill warnings and MCP SSE fallback).
- 2026-03-02T17:03Z [CODE] UNCONFIRMED Python permission prompt in TUI now renders only the code body (no description/mode/source/preview/patterns headers).
- 2026-03-01T16:10Z [CODE] UNCONFIRMED TUI generic tool preview now renders Python inline code without the `[code=...]` wrapper by special-casing the python tool input display.
- 2026-02-27T20:08Z [TOOL] [plan:41-tmux-session-cleanup] tmux environment is clean; no tmux sessions remain.
- 2026-02-27T20:05Z [TOOL] [plan:40-launcher-cwd-render-fix] Launcher-level fix is in place: `~/.local/bin/opencode-fork --port 0` now renders correctly in tmux and pseudo-terminal checks, with active working session in `remote-cli-session:7`.
- 2026-02-27T19:43Z [TOOL] [plan:39-tmux-blank-screen-live-debug] Live tmux remediation applied in place: fork process was relaunched in `remote-cli-session:2` with tmux-specific terminal env and remains running; user validation of on-screen rendering is pending.
- 2026-02-27T19:35Z [TOOL] [plan:38-tmux-fork-session-attach] tmux remote session is live with forked OpenCode running in pane `remote-cli-session:2`; user can attach immediately to interact with the TUI.
- 2026-02-27T19:23Z [TOOL] [plan:37-opencode-fork-black-screen-startup] Black-screen startup triage is resolved: with `--port 0` retained for launch checks and default model corrected to `openai/gpt-5.2-codex`, `opencode-fork` responds normally in shell and tmux-based runs.
- 2026-02-27T17:01Z [TOOL] [plan:36-tmux-ui-load-validation] `opencode-fork` launch path is validated under tmux-cli; observed "UI not loading" symptom in captures is attributable to tmux-cli remote capture limitations rather than a fork startup regression.
- 2026-02-27T16:49Z [TOOL] [plan:35-opencode-fork-cwd-parity] `opencode-fork` now matches normal cwd behavior while still running forked source code.
- 2026-02-27T14:44Z [TOOL] [plan:34-test-opf-alias] Alias intent is correct but current quoting is broken for config-loading commands; verified corrected inline command form sets `.tools.python` to `true` successfully.
- 2026-02-27T14:32Z [TOOL] [plan:33-local-fork-install-and-python-tool-sync] Local fork driving path is ready via `opencode-fork`; global Python tool/runtime dependency state is aligned for current permission-preview behavior without replacing the existing system-installed `opencode` binary.
- 2026-02-27T14:02Z [TOOL] [plan:32-finish-testing-fix-issues] Automated validation is fully green across both workspaces after one PTY isolation fix in forked `opencode`; remaining pre-PR work is user-requested manual TUI self-test/evidence capture and any follow-up cleanup/commit decisions.
- 2026-02-27T13:40Z [TOOL] [plan:31-update-tui-plan-progress] Plan artifacts now match delivered state: implementation phases are marked complete with concrete evidence, and only tmux E2E + PR packaging remain open.
- 2026-02-27T13:36Z [TOOL] [plan:30-delegated-python-permission-implementation-sync] Delegated Python permission implementation work is now continuity-synced across repos: root bounded `codeExpanded` metadata commit (`484f0bd`), forked TUI implementation/fix commits (`bdc1d13`, `9f1bb60`), and final focused validation evidence (`69 pass, 0 fail`) are captured in canonical history.
- 2026-02-27T13:35Z [TOOL] [plan:29-opencode-python-permission-review-followup] Code-review follow-up is complete in forked `opencode`: Python permission body now renders `source`, external_directory Python metadata detection is stricter than mode-only, focused helper tests cover source + negative mode-only classification, focused test command is green, and a new non-amended commit (`9f1bb60`) is recorded.
- 2026-02-27T13:32Z [TOOL] [plan:28-opencode-permission-validation-rerun] Environment dependency resolution and focused permission/config tests are now green in forked `opencode`, increasing confidence that the recent Python permission rendering/config implementation works as intended in this workspace.
- 2026-02-27T13:29Z [TOOL] [plan:27-opencode-python-permission-rendering] Forked `opencode` now contains the Python permission helper, renderer wiring for direct and Python-tagged external_directory prompts, explicit `permission.python` schema key, and focused helper/config tests; commit `bdc1d13` created, but test execution remains blocked locally by unresolved package dependencies/preloads.
- 2026-02-27T13:15Z [TOOL] [plan:26-tui-pr-plan-review] Fresh-eyes review applied 8 improvements to PR plan and test matrix: bounded metadata design (codeFull→codeExpanded), dual-source render precedence, external_directory Python sub-case, config schema registration, test strategy acknowledgement, phase consolidation, rebase timing, and 5 new test scenarios (S5-S9).
- 2026-02-27T12:48Z [TOOL] [plan:25-opencode-tui-pr-plan] Planning artifacts are now in place for future upstream PR work: comprehensive phased implementation plan plus parallel execution/test matrix with commit-safe checkpoints and isolated tmux validation workflow.
- 2026-02-26T18:07Z [TOOL] [plan:24-readme-install-docs-update] README installation docs now align with current `src`-canonical architecture and provide runnable copy/install/validate steps for both minimal and maintainer installs.
- 2026-02-26T18:01Z [TOOL] [plan:23-move-source-to-src] README commit is complete and canonical implementation now resides in `src/` with `.opencode/tool` wrappers; full test suite remains green after relocation.
- 2026-02-26T15:42Z [TOOL] [plan:22-comprehensive-readme] README now serves as a comprehensive, technically aligned guide for adopting and operating this custom OpenCode Python tool, including security posture, runtime semantics, verification workflow, and downstream sync guidance.
- 2026-02-26T15:38Z [TOOL] [plan:21-readme-deny-rationale] README documentation now explicitly recommends default Python deny statements and explains the security impact of not denying dangerous dynamic execution/process/deserialization patterns.
- 2026-02-26T13:55Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Phase 16 is implemented and validated: tool wording now routes heredoc-intent Python execution to the `python` tool, direct `subprocess.*` usage hard-fails before any permission ask with `bash` reroute guidance, runtime tests are green, and plan/continuity docs reflect `DONE` status.
- 2026-02-26T13:44Z [TOOL] [plan:19-phase15-permission-code-visibility] Phase 15 is implemented and validated: permission asks now include execution-mode context and bounded executable-source preview metadata (with deterministic truncation + size counters), runtime behavior is unchanged, tests are green, and plan status reflects `DONE`.
- 2026-02-26T13:40Z [TOOL] [plan:20-phase16-routing-subprocess-hardfail] Plan now includes a dedicated Phase 16 for heredoc-to-python-tool routing guidance and immediate subprocess rejection behavior prior to implementation.
- 2026-02-26T13:32Z [TOOL] [plan:19-phase15-permission-code-visibility] Plan now includes a dedicated Phase 15 to add executable code preview metadata to Python permission asks before implementation changes.
- 2026-02-26T13:24Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Phase 14 is implemented and validated: Atlassian instance normalization is now flow-aware with reassignment invalidation, bare constructors require import provenance, module-qualified constructor variants are consistently covered for supported families, regression tests are green, and plan/continuity docs reflect `DONE` status.
- 2026-02-26T13:20Z [TOOL] [plan:18-phase14-atlassian-precision-hardening] Plan now includes a dedicated remediation phase (Phase 14) for code-review precision fixes before further analyzer/runtime changes.
- 2026-02-26T13:05Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Phase 13 is implemented and validated: Atlassian constructor call surfaces classify as `exec`, tracked instance methods classify as normalized `exec:atlassian.<client>.<method>`, fallback/alias deferral semantics are preserved, tests pass, and plan status now reflects `DONE`.
- 2026-02-26T13:00Z [TOOL] [plan:17-phase13-atlassian-phase-plan] Plan now includes a dedicated Atlassian Python API coverage phase (Phase 13) without changing analyzer/runtime implementation in this step.
- 2026-02-25T20:58Z [TOOL] [plan:16-phase12-ghapi-coverage] Phase plan tracking now reflects completion through Phase 12, with OCI and GHAPI coverage explicitly marked done and verified.
- 2026-02-25T20:57Z [TOOL] [plan:16-phase12-ghapi-coverage] Phase 12 GHAPI analyzer coverage is implemented and validated: known GHAPI remote call surfaces now classify as `exec`, workflow helper calls classify as `write`, bounded `GhApi` instance-variable normalization emits `ghapi.<group>.<verb>` exec identities, tests are expanded and passing, and no beyond-Phase-12 work was started.
- 2026-02-25T20:39Z [TOOL] [plan:15-phase11-oci-coverage] Phase 11 OCI analyzer coverage is implemented and validated: known OCI SDK entrypoints and chained client method calls now classify as `exec`, `oci.config.from_file` now classifies as `read` with literal/dynamic path behavior, analyzer tests are expanded and passing, and Phase 12 (GHAPI) was not started.
- 2026-02-25T19:21Z [TOOL] [plan:14-phase10-docs-continuity] Phase 10 documentation/continuity finalization is complete: plan status tracking now matches completed implementation through Phase 9 plus this documentation pass, known limitations are current, and future phases 11-12 are intentionally left pending.
- 2026-02-25T18:08Z [TOOL] [plan:13-phase9-policy-hardening] Phase 9 permission policy hardening is implemented and validated: dangerous Python deserialization exec patterns are now explicitly denied, restrictive defaults and unknown/external-directory ask posture are preserved, and no Phase 10+ work was started.
- 2026-02-25T17:29Z [TOOL] [plan:12-phase8-runtime-tests] Phase 8 runtime test suite is implemented and passing; coverage now validates permission planning, external directory asks, runtime execution behavior, timeout/abort metadata, streaming updates, ENOENT ergonomics, and workdir resolution without beginning Phase 9+ policy changes.
- 2026-02-25T17:05Z [TOOL] [plan:11-phase7-analyzer-tests] Phase 7 analyzer test suite is implemented and passing with deterministic assertions, fixture helper is present, callable-fallback expectations now consistently use `unknown:callable:<name>`, and no Phase 8+ implementation work was started.
- 2026-02-25T16:43Z [TOOL] [plan:10-phase6-runtime-hardening] Phase 6 runtime hardening is implemented and validated; venv resolution, prompt hardening, script-path ENOENT clarity, env hardening, and non-zero exit metadata are in place without starting Phase 7+ test-suite expansion.
- 2026-02-25T16:38Z [TOOL] [plan:09-ghapi-phase-plan] Plan now includes a dedicated GHAPI coverage phase, without changing implementation code in this step.
- 2026-02-25T16:33Z [TOOL] [plan:08-oci-phase-plan] Plan now explicitly tracks OCI SDK coverage as a dedicated future phase, without changing implementation code in this step.
- 2026-02-25T16:30Z [TOOL] [plan:07-update-plan-callable-fallback] Plan document is now aligned with implemented callable-fallback behavior; future phases reference callable-specific unknown permission patterns instead of `unknown:no-classified-call` for unrecognized calls.
- 2026-02-25T16:25Z [TOOL] [plan:06-callable-unknown-fallback] Generic callable fallback classification is implemented and validated; unknown calls now produce callable-identity events and permission patterns, while parse-error/empty-source/no-calls semantics remain intact.
- 2026-02-25T15:41Z [TOOL] Bootstrap completed for cwd-based development; implementation now targets root `.opencode` workspace instead of `./opencode`.
- 2026-02-25T15:47Z [TOOL] CWD workspace now contains both implementation and plan artifacts; `opencode/` remains reference-only and ignored by root git.
- 2026-02-25T16:18Z [TOOL] [plan:05-analyzer-hardening] Phase 5 is implemented and validated; Phase 6+ intentionally not started pending user review gate.
- 2026-02-25T16:17Z [TOOL] [plan:05-analyzer-hardening] Phase 5 analyzer hardening completed and validated without implementing Phase 6+ work.
- 2026-03-05T15:27Z [TOOL] 2026-03-05T15:35Z PII remediation complete (P0-P3 all done). Final verification scan PASS: no real names, corporate emails, absolute home paths, GitHub account refs, or secrets in tracked files or git history. Repo is ready for public push.
- 2026-03-05T20:33Z [TOOL] [plan:42-python-tool-output-visibility] Phase 1 is complete and documented in the active plan with Notes; focused verification passed (`bun test test/permission-python.test.ts`, `bun run typecheck`), Phase 2 remains pending, and no commit was created.
- 2026-03-05T20:43Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:05Z Delivered focused review of `session/index.tsx` and plan doc: no edits made; verdict Needs Fixes with one medium correctness gap (metadata/output mismatch precedence) and one low doc-status consistency issue.
- 2026-03-05T20:44Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:05Z Delivered ranked code-review verdict (Needs Fixes) with concrete line-level findings and targeted tests for Python TUI renderer parity/edge cases; no code edits performed.
- 2026-03-05T20:46Z [TOOL] [plan:42-python-tool-output-visibility] Delivered focused review verdict Needs Fixes with ranked findings, targeted remediations, and regression test recommendations; no code edits made.
- 2026-03-05T20:47Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T20:45Z Review verdict: Needs Fixes for Phase 1 before closure; provided concrete code/test/doc recommendations without modifying repository files.
- 2026-03-05T20:51Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:20Z Read-only verification of updated Phase 1 implementation and plan doc returned Approve: no unresolved correctness/privacy/parity issues found in scoped files; plan status now aligns with Phase 1 DONE + Phase 2 TODO.
- 2026-03-05T20:51Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:12Z Read-only re-review of updated Phase 1 (`session/index.tsx`, `python-tool-output-visibility-plan.md`) verifies prior blockers are resolved: pending-state parity, completed output precedence, Bash-aligned workdir privacy when base path missing, and plan status consistency. Verdict: Approve.
- 2026-03-05T20:51Z [TOOL] [plan:42-python-tool-output-visibility] 2026-03-05T21:15Z Review verdict for updated Phase 1 is Approve with no remaining blockers; recommended only focused regression tests for Python output precedence/state combinations.
- 2026-03-05T21:22Z [TOOL] [plan:42-python-tool-output-visibility] Prepared recommended Phase 2 approach: extract/test pure Python renderer helpers, add focused Bun tests for precedence/inline-script/empty-long output behavior, and update plan/docs notes with validation commands and acceptance mapping.
- 2026-03-05T21:27Z [TOOL] [plan:42-python-tool-output-visibility] Plan 42 is complete: both phases are done, focused validation passed (`bun test test/cli/tui/python-output.test.ts test/permission-python.test.ts`, `bun run typecheck`), and no new commit was created for Phase 2.
- 2026-03-05T21:42Z [TOOL] [plan:42-python-tool-output-visibility] Plan 42 is implemented in two commits: Phase 1 `806ee27` and Phase 2 `660893e`; focused validation remains green and `permission.tsx` local edits remain uncommitted/out-of-scope.
- 2026-03-05T21:58Z [TOOL] Updated global Python tool import path in `~/.config/opencode/tools/python.ts` to `@opencode-ai/plugin` and verified load success with `cd ~/.config/opencode && bun -e "import('./tools/python.ts').then(() => console.log('python tool loads ok'))"`.

## [THEMES]

## [MILESTONES]
