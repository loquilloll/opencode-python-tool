# CONTINUITY

## [PLANS]
- 2026-03-18T17:42Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-18T17:25Z Added `docs/plans/77-python-module-family-review-batching.md`, a new active plan for module- and family-oriented review batching on top of `src/python-session-report.ts` so repeated candidates can be grouped by canonical source and reviewed through representative snippets instead of one-by-one.
- 2026-03-27T11:41Z [USER] [plan:78-python-analyzer-test-modularization] User requested saving a new build plan to split `.opencode/test/python-analyze.test.ts` into focused analyzer test files and starting implementation immediately.
- 2026-03-27T16:38Z [USER] [plan:79-python-analyzer-test-naming-cleanup] User requested starting a small follow-up cleanup plan for analyzer test naming debt after plan 78, especially around the broad `python-analyze-exec-network.test.ts` host and preserved legacy suite labels.
- 2026-03-29T23:48Z [CODE] [plan:80-python-live-unknown-classification-buckets] Created and completed plan 80 for the new live unknown queue after plan 77 closed; the follow-up covered exact stdlib trust-gating, bounded Path provenance, conservative review-ledger closeout, and doc updates.
- 2026-03-30T21:13Z [USER] [plan:81-python-reopened-family-classification] User asked to save a new plan for the reopened unknown-family queue and implement its first phase.

## [DECISIONS]
- 2026-03-29T16:23Z [TOOL] Made both workspace and global OpenCode configs tolerant to permission-key matcher differences by allowing both `read*`/`read:*`, `emit*`/`emit:*`, and `pure*`/`pure:*`. If prompts persist after this, the remaining likely cause is process-level config caching, which requires a full OpenCode app restart rather than only restarting the chat session.
- 2026-03-29T23:48Z [CODE] [plan:80-python-live-unknown-classification-buckets] Plan 80 keeps analyzer widening exact and bounded: `os.walk`, `fnmatch.fnmatch`, `struct.unpack`, and builtin `int.from_bytes` are trusted only for module-qualified or exact direct-import forms where applicable, while the remaining singleton queue tail is closed in the review ledger instead of broadening `open.read` or pattern/match string provenance.
- 2026-03-30T21:26Z [CODE] [plan:81-python-reopened-family-classification] Finished Phase 1 by narrowing it to bounded local string producers only. The remaining originally listed string families (`statement.lower`, `stmt.lower`, `stmt.lower.strip`, `p.name.lower`) are re-scoped out of Phase 1 because their live representatives come from OCI response-object attributes (`policy.statements`, `policy.name`) rather than same-scope local string proofs.
- 2026-03-30T22:33Z [CODE] [plan:81-python-reopened-family-classification] Phase 2 stays producer-side only: conditional candidate lists and empty-list `append(...)` / `+= [...]` builders can keep `cand.exists` / `cand.is_file` Path-backed only when every carried branch/member is already Path-backed. Direct conditional receivers and arbitrary `.exists()` / `.is_file()` objects remain conservative.
- 2026-03-31T11:59Z [CODE] [plan:81-python-reopened-family-classification] Phase 3 uses a bounded OCI seam only for direct `policy.statements` loops from trusted `oci.identity.IdentityClient` + `oci.pagination.list_call_get_all_results(...).data`. It intentionally stops before `p.name.lower` because the remaining `sorted(..., key=lambda p: p.name.lower())` family would require broader lambda/object-field propagation than this phase allows.
- 2026-03-31T13:21Z [CODE] [plan:81-python-reopened-family-classification] Phase 4 closed the remaining singleton/custom-instance tails in the review ledger instead of adding new analyzer rules. Ledger decisions treated OCI fetches like `identity.get_tenancy` and `network.get_vcn` as read-style operations, file/archive writes like `wb.save`, `ZipFile`, `zf.writestr`, and `py_compile.compile` as write, in-memory helpers like `Workbook`, `Font`, `escape`, list appends, and `GitHubApi` construction as pure, and repo-local GitHub wrapper mutations as write.
- 2026-03-31T13:37Z [CODE] [plan:81-python-reopened-family-classification] Phase 5 closes plan 81 with an explicit split between analyzer-fixed families (`str.startswith`, `cand.exists`, `cand.is_file`, `statement.lower`, `stmt.lower`, `stmt.lower.strip`) and ledger-only tails (`p.name.lower`, OCI fetches, openpyxl/zipfile/file helpers, and repo-local GitHub wrappers). Future recurrence of ledger-only tails should start a fresh plan instead of reopening plan 81.

## [PROGRESS]
- 2026-03-26T20:07Z [CODE] [plan:77-python-module-family-review-batching] Resolved the `old_path.exists` / `old_path.rename` bucket by extending iterated Path provenance for explicit tuple/list Path-pair destructuring, then bounding it to flat identifier targets with alias- and subscript-safe invalidation.
- 2026-03-26T20:50Z [CODE] [plan:77-python-module-family-review-batching] Resolved the `m.group.endswith` / `m.group.replace` bucket by making bounded single-selector regex `group(...)` / `__getitem__()` returns string-backed, which lets downstream `.endswith()` and `.replace()` chains reuse the existing string receiver classification.
- 2026-03-26T22:52Z [CODE] [plan:77-python-module-family-review-batching] Added iterative helper seed/return discovery plus tracked identifier container lookup so bounded helper-return datetime chains can feed existing receiver purity and helper-local timedelta arithmetic. Updated analyzer/runtime tests and classifier reference docs for the new boundary.
- 2026-03-27T11:41Z [CODE] [plan:78-python-analyzer-test-modularization] Created `docs/plans/78-python-analyzer-test-modularization.md` and completed Phase 1 by splitting parity/evidence coverage into `.opencode/test/python-analyze-parity.test.ts` and rules-loader coverage into `.opencode/test/python-analyze-rules-loading.test.ts`, trimming `.opencode/test/python-analyze.test.ts` to the remaining suites.
- 2026-03-27T12:00Z [CODE] [plan:77-python-module-family-review-batching] Fixed the stale `hashlib.sha256` family-cluster regression in `.opencode/test/python-session-report.test.ts` by updating the merged-occurrence test to scan with `includePure: true` instead of changing `reviewFamilies()` or analyzer behavior.
- 2026-03-27T12:14Z [CODE] [plan:78-python-analyzer-test-modularization] Completed Phase 2 by moving `open()`, `Path`, `os/shutil`, and `json/yaml` suites into `.opencode/test/python-analyze-paths-io.test.ts`, leaving `.opencode/test/python-analyze.test.ts` starting at `emit and pure classification`.
- 2026-03-27T13:02Z [CODE] [plan:78-python-analyzer-test-modularization] Completed Phase 3 by moving `emit and pure classification` into `.opencode/test/python-analyze-pure-core.test.ts` and the contiguous inspect/datetime/time/calendar/Counter/hashlib/zlib/difflib suites into `.opencode/test/python-analyze-library-pure.test.ts`, leaving `.opencode/test/python-analyze.test.ts` starting at `github classification`.
- 2026-03-27T13:30Z [CODE] [plan:78-python-analyzer-test-modularization] Completed Phase 4 by deleting `.opencode/test/python-analyze.test.ts` after moving the remaining suites into `.opencode/test/python-analyze-exec-network.test.ts`, `.opencode/test/python-analyze-integrations.test.ts`, and `.opencode/test/python-analyze-fallbacks.test.ts`.
- 2026-03-27T16:29Z [CODE] [plan:78-python-analyzer-test-modularization] Completed Phase 5 by updating `README.md`, `docs/python-classification-system-design.md`, and `docs/python-custom-tool-plan.md` to reference the split analyzer test layout, then marked `docs/plans/78-python-analyzer-test-modularization.md` done.
- 2026-03-27T16:38Z [CODE] [plan:79-python-analyzer-test-naming-cleanup] Created `docs/plans/79-python-analyzer-test-naming-cleanup.md` as the active build plan for behavior-first suite naming cleanup across parity, exec-network, integrations, and library-pure analyzer test files.
- 2026-03-27T16:55Z [CODE] [plan:79-python-analyzer-test-naming-cleanup] Refined `docs/plans/79-python-analyzer-test-naming-cleanup.md` with an explicit old->new suite-title mapping table, tighter validation commands, and content-based wording for the two duplicate zlib suites so implementation can proceed mechanically without guessing names.
- 2026-03-27T17:59Z [CODE] [plan:79-python-analyzer-test-naming-cleanup] Implemented Phase 1 by renaming the in-scope analyzer `describe(...)` hosts in parity, exec-network, integrations, and library-pure suites, then marked `docs/plans/79-python-analyzer-test-naming-cleanup.md` done with the final old->new mapping.
- 2026-03-27T20:29Z [CODE] [plan:77-python-module-family-review-batching] Added `.opencode/skills/python-family-batching/scripts/analyzer_probe.sh`, extended `common.sh` with a repo-local analyzer runner, documented the helper in `SKILL.md`, and updated `.opencode/opencode.jsonc` with explicit analyzer-probe script allow rules.
- 2026-03-28T13:29Z [CODE] [plan:77-python-module-family-review-batching] Extended `python-replay`/`python-inference-containers`/`python-provenance`/`python-scope` so datetime tuple destructuring, exact-key dict reads, and pair-loop `total_seconds()` can reuse existing pure method classification. Added focused analyzer/runtime regressions in the split library-pure/basic suites and refreshed the classification reference prose.
- 2026-03-28T22:16Z [CODE] [plan:77-python-module-family-review-batching] Drained the remaining live `ts.isoformat` / `a.isoformat` families by extending tuple-slot carriers through enumerate-backed tuples, nested tuple siblings, and helper calls inside comprehension scopes. Added bounded positive-membership narrowing for inline conditional expressions over exact iterables, which reduced `item.lower` from two live snippets to one unguarded remaining case.
- 2026-03-28T23:23Z [CODE] [plan:77-python-module-family-review-batching] Hardened key-equivalence narrowing for negative membership, scalar-string membership, unrelated guard names, boolean `and`/`or`, and inner-scope shadowing. Added helper-string exact-value seeds so reduced repros for helper-backed exact-key `total_seconds()` now classify as pure.
- 2026-03-29T11:41Z [CODE] [plan:77-python-module-family-review-batching] Extended plan-77 follow-up coverage with typed-helper parameter handling and nested tuple-subscript datetime reads (`pts[i][0]`). After validating the homogeneous `total_seconds` representative with analyzer probes, recorded a local `pure` review decision for the remaining `total_seconds` family.
- 2026-03-29T13:01Z [CODE] Implemented `--analyzer-rules` parsing/help/defaults in `src/python-session-report.ts`, added CLI coverage plus a scan-time classification regression in `.opencode/test/python-session-report.test.ts`, and updated README/operator-guide plus active skill docs/scripts to prefer the new flag for `python-session-report` invocations.
- 2026-03-29T13:04Z [CODE] Updated historical plan-note command examples in `docs/plans/54-python-session-history-provenance-audit.md`, `docs/plans/55-python-response-json-provenance.md`, and `docs/plans/77-python-module-family-review-batching.md` to use `--analyzer-rules` for `src/python-session-report.ts` instead of exporting `OPENCODE_PYTHON_RULES` inline.
- 2026-03-29T13:11Z [TOOL] [plan:77-python-module-family-review-batching] Ran `src/python-session-report.ts --review-next --family item.lower`, reviewed the single remaining tfvars-shaped snippet, and applied `--decide 1=pure`. Follow-up family checks now show no pending `item.lower` or `total_seconds` items.
- 2026-03-29T15:18Z [TOOL] [plan:77-python-module-family-review-batching] Drained the remaining live queue by combining bounded analyzer fixes (direct regex list returns, iterated regex match receivers, lambda local-definition filtering, guarded snapshot hardening) with explicit review-ledger decisions for residual singleton families.
- 2026-03-29T23:48Z [CODE] [plan:80-python-live-unknown-classification-buckets] Extended Path provenance through helper params, `list(...).pop()` stacks, empty-list append/extend candidate builders, sorted path unions, and call-form/bound-alias `__setitem__` invalidation. That cut the live queue from the point-in-time 26 families down to 13 before the final review-ledger decisions drained the rest.
- 2026-03-30T20:26Z [TOOL] Used the review workflow (`--review-families-json` plus `review_snippets.sh`) to identify the current unknown-call backlog rather than changing classifier rules. Representative snippets cluster into OCI SDK calls (`identity.get_tenancy`, `network.get_vcn`), workbook/archive APIs (`Workbook`, `wb.save`, `ZipFile`), Path/provenance gaps (`cand.exists`, `cand.is_file`, `fh.read`), and string-local provenance (`statement.lower`, `str.startswith`).
- 2026-03-30T21:13Z [CODE] [plan:81-python-reopened-family-classification] Created plan 81 and implemented a safe Phase 1 sub-slice by teaching bounded builtin `str(...)` results to seed downstream string receiver methods. That cleared the reopened `str.startswith` family without widening generic string or attribute handling.
- 2026-03-30T21:26Z [CODE] [plan:81-python-reopened-family-classification] Updated plan 81 so Phase 1 is now done, Phase 3 is an optional OCI-specific bounded residual phase, and Phase 4 explicitly owns ledger closeout for the OCI policy-object string families if no narrow SDK seam emerges.
- 2026-03-30T22:33Z [CODE] [plan:81-python-reopened-family-classification] Completed Phase 2 by extending `iteratedPathValue()` to handle conditional path-candidate lists and teaching replay to preserve seeded Path-list identity across bounded `+=` list augmentation. Added matching analyzer/runtime regressions for positive, mixed, and post-invalidation cases.
- 2026-03-31T11:59Z [CODE] [plan:81-python-reopened-family-classification] Implemented Phase 3 by adding trusted OCI identity-client and paginated policy-list tracking, revoking that trust on list mutation, subscript replacement, and OCI root/method monkeypatching, and adding focused analyzer/runtime regressions for positive, fake-producer, mutation, alias, and monkeypatch cases.
- 2026-03-31T13:21Z [CODE] [plan:81-python-reopened-family-classification] Implemented Phase 4 by draining the remaining queue through `--review-next --family ... --decide ...` review-ledger decisions after representative snippet inspection. This included the deferred `p.name.lower` OCI tail plus the remaining openpyxl/zipfile/file-helper and GitHub-wrapper families.

## [DISCOVERIES]
- 2026-03-30T21:13Z [TOOL] [plan:81-python-reopened-family-classification] The remaining planned Phase 1 families are not the same shape as `str.startswith`: `statement.lower`, `stmt.lower`, `stmt.lower.strip`, and `p.name.lower` are still blocked because the live representatives come from OCI/object attributes (`policy.statements`, `policy.name`) rather than same-scope local string producers.
- 2026-03-30T21:13Z [TOOL] [plan:81-python-reopened-family-classification] Focused validation is green for the Phase 1 slice: `bun test test/python-analyze-pure-core.test.ts` (73 pass) and `bun test test/python-inline-permissions-basic.test.ts` (126 pass). `--review-families-json --family str.startswith` now returns 0 pending items, while `--family statement.lower` still reports 4 pending candidates across 4 snippets.
- 2026-03-30T21:26Z [TOOL] [plan:81-python-reopened-family-classification] Representative snippets confirm the Phase 1 leftovers are OCI response-object chains: `statement.lower` and `stmt.lower.strip` come from `for statement in policy.statements` / `for stmt in policy.statements`, while `p.name.lower` comes from `sorted(policies, key=lambda p: p.name.lower())`. Those families are not same-shape local string producers like the cleared builtin `str(...).startswith(...)` case.
- 2026-03-31T11:59Z [TOOL] Compaction triggered: exceeded upper token threshold; truncated oldest entries to at or below lower target; archived to docs/MEMORY.md.
- 2026-03-30T22:33Z [TOOL] [plan:81-python-reopened-family-classification] Focused validation is green for the Phase 2 slice: `bun test test/python-analyze-paths-io.test.ts` (47 pass) and `bun test test/python-inline-permissions-inference.test.ts` (64 pass). `verify_bucket.sh --test test/python-analyze-paths-io.test.ts --family cand.exists --family cand.is_file --snapshot` now reports no pending review items for either family and a point-in-time overall queue of `15` snippets / `43` pending candidates / `31` families.
- 2026-03-31T11:59Z [TOOL] [plan:81-python-reopened-family-classification] Focused validation is green for the Phase 3 slice: `bun test test/python-analyze-pure-core.test.ts` (74 pass) and `bun test test/python-inline-permissions-basic.test.ts` (127 pass). `--review-families-json` now reports `0` pending families for `statement.lower`, `stmt.lower`, and `stmt.lower.strip`, while `p.name.lower` remains at `2` pending candidates across `2` snippets.
- 2026-03-31T13:21Z [TOOL] [plan:81-python-reopened-family-classification] Final Phase 4 verification is point-in-time clean: `bun run ../src/python-session-report.ts --analyzer-rules "/home/alvins/Documents/pgit/opencode-python-tool/src/python/python-rules.json" --review-families-json` and `.opencode/skills/python-family-batching/scripts/queue_snapshot.sh --limit 40 --include-representatives` both now report `0` snippets / `0` pending candidates / `0` families.

## [OUTCOMES]
- 2026-03-29T13:04Z [TOOL] The requested follow-up is complete for historical plan notes: remaining session-report examples in plan docs now match the new CLI flag form. Verification was docs-focused (`grep`), so no test suite was needed for this pass.
- 2026-03-29T13:11Z [TOOL] [plan:77-python-module-family-review-batching] The user-requested targeted cleanup is fully addressed: the startswith guard blocker is closed, `total_seconds` is drained, and the last `item.lower` family is cleared via a documented review-ledger exception. Remaining queue work is now outside the targeted leftovers for this pass.
- 2026-03-29T15:18Z [TOOL] [plan:77-python-module-family-review-batching] Plan 77 is complete. Phase 5 is now done, the review queue is empty, and the plan closed without a commit; any further review batching or queue cleanup should start under a new plan rather than extending plan 77.
- 2026-03-29T15:21Z [CODE] [plan:77-python-module-family-review-batching] Updated `docs/plans/77-python-module-family-review-batching.md` frontmatter and tags from active to done, with `updated: 2026-03-29`, to match the drained queue and Phase 5 completion.
- 2026-03-29T15:29Z [TOOL] [plan:77-python-module-family-review-batching] Created commit `7328c59` (`feat: drain python family batching queue`) for the plan-77 closeout slice after staging the analyzer/runtime/docs changes and leaving unrelated workspace changes (`.opencode/package.json`, `.opencode/bun.lock`, untracked docs/tooling files) uncommitted.
- 2026-03-29T15:52Z [TOOL] Re-synced the global Python tool install in `~/.config/opencode/tools/`, created fresh timestamped backups for overwritten files, copied the missing `python-key-equivalence.ts`, and verified the install with `cd ~/.config/opencode && bun install && bun -e "import('./tools/python.ts').then(() => console.log('python tool loads ok'))"`.
- 2026-03-29T16:25Z [TOOL] After a full OpenCode process restart, live permission probes behaved as intended in this repo session: an in-worktree dynamic read completed without a prompt, `print('emit-ok')` completed without a prompt, and pure-only code (`re.search`, `sum`) completed without a prompt.
- 2026-03-29T23:48Z [TOOL] [plan:80-python-live-unknown-classification-buckets] Plan 80 is done. Final point-in-time live snapshot from `.opencode/skills/python-family-batching/scripts/queue_snapshot.sh --limit 30 --include-representatives` is `0` snippets / `0` pending candidates / `0` families, and the plan closed without a commit.
- 2026-03-30T14:25Z [TOOL] Synced the global OpenCode Python tool install under `/home/alvins/.config/opencode/tools` from the repo's full 26-file runtime set, created timestamped backups `*.bak.20260330T142022Z` for the 9 changed files, verified all copied files byte-match the repo sources, and passed `cd ~/.config/opencode && bun install && bun -e "import('./tools/python.ts').then(() => console.log('python tool loads ok'))"`.
- 2026-03-30T22:33Z [TOOL] [plan:81-python-reopened-family-classification] Phase 2 is done. The `cand.exists` and `cand.is_file` families are cleared without widening arbitrary receiver methods, docs/plan notes are updated, and the next decision is whether to attempt the optional OCI-specific Phase 3 or skip directly to Phase 4 ledger closeout.
- 2026-03-31T11:59Z [TOOL] [plan:81-python-reopened-family-classification] Phase 3 is done as a bounded OCI loop-field slice. It clears the policy-statement string families without leaking generic `.name` / `.statements` trust, leaves `p.name.lower` intentionally deferred to Phase 4, and the latest read-only review approves the slice with no blocker/significant issues.
- 2026-03-31T13:21Z [TOOL] [plan:81-python-reopened-family-classification] Phase 4 is done. The reopened review queue is fully drained via the intended mix of bounded analyzer changes (Phases 1-3) and ledger-only closeout (Phase 4), with Phase 5 left only for plan-closeout bookkeeping if desired.
- 2026-03-31T13:37Z [TOOL] [plan:81-python-reopened-family-classification] Plan 81 is complete. Final validation at 2026-03-31T13:34Z/13:20Z shows `queue_snapshot.sh --limit 20 --include-representatives` and `bun run ../src/python-session-report.ts --analyzer-rules "/home/alvins/Documents/pgit/opencode-python-tool/src/python/python-rules.json" --review-families-json` both report `0` snippets / `0` pending candidates / `0` families; Phase 5 updated the plan status to done and recorded the analyzer-vs-ledger closeout split.
