# CONTINUITY

## [PLANS]
- 2026-03-10T18:54Z [USER] [plan:72-python-review-identity-and-promotion-safety] Requested a saved implementation plan for review-identity ledger/reuse/promotion changes in `src/python-session-report.ts` based on a patch-ready proposal that replaces bare call-level reuse with `kind` plus canonical callable context.
- 2026-03-13T00:52Z [USER] [plan:73-python-analyzer-modularization] User requested writing a new implementation plan file for breaking up `src/python/python-analyze.ts` into smaller modules.
- 2026-03-17T13:08Z [USER] [plan:76-python-permission-provenance-view] Requested a read-only, commit-safe implementation plan for adding provenance to the Python permission preview/allow view across `src/python.ts`, TUI permission helper/renderer, focused runtime/TUI tests, and concise doc touchpoints.
- 2026-03-17T13:11Z [USER] [plan:76-python-permission-preview-provenance] Requested writing a new implementation plan for showing compact provenance context in the Python permission preview/allow view.
- 2026-03-18T17:42Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-18T17:25Z Added `docs/plans/77-python-module-family-review-batching.md`, a new active plan for module- and family-oriented review batching on top of `src/python-session-report.ts` so repeated candidates can be grouped by canonical source and reviewed through representative snippets instead of one-by-one.

## [DECISIONS]
- 2026-03-23T20:23Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-23T19:25Z Follow-on hardening supersedes the earlier root-wide dict-subscript experiment: helper exact receiver-path string seeds are now scope-aware and helper-only, while direct dict-subscript iteration stays conservative until a persisted exact-path proof can be carried into the main pass.
- 2026-03-24T12:11Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T12:11Z Phase 5 now persists exact same-syntax dict-subscript string-list provenance into the main pass through `receiverElementKinds`, but it stays intentionally raw-path keyed. We explicitly do not canonicalize `inv[current]` to `inv['literal']`; path-mismatched reads remain conservative until value-level key canonicalization is proven safe.
- 2026-03-24T13:54Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T13:53Z Phase 5 now has a second-tier exact-key proof: direct dict-subscript reads may reuse persisted string-list provenance across value-equivalent exact string keys such as `inv[current]` and `inv['literal']`, but only when the key literal is proven exactly. Derived-key flows (for example `line.split(...)[1].strip()`) remain conservative until exact value propagation is added.
- 2026-03-24T14:20Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:20Z Phase 5 now allows a bounded exact string-value layer on top of exact-key equivalence: pure string derivations (`split`/`rsplit`/`splitlines` indexing plus `strip`-family cleanup) and iterated exact literal-line sources may seed exact dict keys, but mixed-line loops and dynamic-origin strings still stay conservative without branch/value narrowing.
- 2026-03-24T15:25Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:27Z Phase 5 now also applies bounded `startswith` branch narrowing for exact string sets inside `if`/`elif` bodies. Mixed literal-line loops can seed exact dict keys when the active branch proves the line shape, but dynamic file-backed loop sources still stay conservative.
- 2026-03-24T15:39Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:40Z After exhausting the bounded `e.split` / `e.startswith` provenance work, the next blocked string receiver bucket does not need new method-family rules: `text.count` / `text.endswith` were already pure once the receiver was proven string, so the safe fix is to extend the missing producer-side path provenance rather than widen string classification.
- 2026-03-24T16:41Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T15:05Z Trusted exact-call gating is now centralized through `TRUSTED_CALL_POLICIES` plus `trustedCallWithPolicy()` instead of duplicated OR chains. Adding new trusted module-call families should now be data-first in the registry rather than branch-first in classifier/inference code.
- 2026-03-24T17:54Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T17:05Z Exact-call trust policy is now split cleanly: `module-qualified-or-exact-direct-import` covers cases like `importlib.import_module`, while `exact-direct-import` is now enforced literally for families like `kiota_abstractions.request_information.RequestInformation` and `tabulate.tabulate`. This keeps policy names aligned with behavior and avoids silently widening direct-import-only families.

## [PROGRESS]
- 2026-03-19T11:35Z [CODE] [plan:77-python-module-family-review-batching] Completed Phase 3 by adding selector parsing/help/validation, `selectReviewFamilies()` and `filterReviewQueue()` in `src/python-session-report.ts`, filtered family-summary reports, module-aware drilldown rendering, representative-first queue ordering, focused tests, and small README/operator-guide selector examples.
- 2026-03-19T12:08Z [CODE] [plan:77-python-module-family-review-batching] Completed Phase 4 by tightening README and operator-guide workflow wording around recommendation labels, batchable-vs-provenance-heavy targets, selector constraints, family-first validation with `--suggest-rules`/`--compare-rules`, and the promotion/rerun sequence; updated plan 77 notes and status to mark the docs phase done.
- 2026-03-19T12:38Z [CODE] [plan:77-python-module-family-review-batching] Implemented the first family patch from the batching workflow by updating `src/python/python-rules.json` and `src/python/python-scope.ts`, then adding analyzer/runtime regressions and docs for `pytest.main`.
- 2026-03-19T16:57Z [CODE] [plan:77-python-module-family-review-batching] Added trust propagation for imported module roots and trusted aliases in `src/python/python-scope.ts` / `src/python/python-analyze.ts`, then consumed that trust in `src/python/python-classifier.ts` and `src/python/python-inference.ts` so the new `pytest.main` and `hashlib.sha256` family rules stay narrow under shadowing and alias rebinding.
- 2026-03-19T20:18Z [TOOL] 2026-03-19T20:15Z In nested `opencode` repo, fetched `origin`/`upstream`, found local `dev` diverged after `origin/dev` was force-updated, then rebased the 7 local TUI/Python commits onto `origin/dev`; resolved the single `packages/opencode/src/pty/index.ts` conflict by preserving the newer `Instance.bind`/`remove(id)` lifecycle while applying the websocket `{data, send}` identity guard from `fix(pty): prevent output leak when websocket state mutates`.
- 2026-03-19T20:31Z [TOOL] 2026-03-19T20:25Z After the rebase/push dry run, fixed the unrelated `opencode` pre-push blocker by widening `packages/opencode/src/installation/index.ts` upgrade-result typing from the narrow `upgradeCurl()` return shape to `Process.Result`, matching the `Process.run()`/`Process.text()` branches assigned in the same switch.
- 2026-03-19T20:48Z [TOOL] 2026-03-19T20:45Z In nested `opencode` repo, restored local `dev` to `4ee426ba549131c4903a71dfb6259200467aca81` (`v1.2.27`) while preserving the 7 user TUI/Python commits by creating backup branch `backup/dev-before-4ee426-20260319-1`, cherry-picking `923d724dd..24f52b6cf` onto the target commit, and then moving `dev` to that rewritten history without touching `origin/dev`.
- 2026-03-19T21:01Z [TOOL] 2026-03-19T21:00Z Created local branch `python-tool` from the restored local history (`v1.2.27` plus the user's 7 commits). Initial push was blocked by the repo's pre-push `bun turbo typecheck` hook because the restored branch no longer included the earlier installation typing fix.
- 2026-03-19T21:32Z [TOOL] 2026-03-19T21:35Z Updated local launcher `~/.local/bin/opencode-fork` to default `OPENCODE_DISABLE_CHANNEL_DB=1` while preserving caller cwd behavior, so the fork now uses the shared `opencode.db` unless the env var is explicitly overridden.
- 2026-03-20T15:48Z [CODE] [plan:77-python-module-family-review-batching] Added Counter receiver tracking in `src/python/python-provenance.ts`, `src/python/python-known-methods.ts`, and `src/python/python-inference.ts`, plus importlib exact-family rules/direct-import handling in `src/python/python-rules.json`, `src/python/python-scope.ts`, and `src/python/python-classifier.ts`, with focused analyzer/runtime regressions and reference-doc updates.
- 2026-03-20T16:44Z [CODE] [plan:77-python-module-family-review-batching] Added the remaining negative runtime coverage requested by recent reviews (`pytest.approx`, aliased importlib forms, captured Counter constructor names), then implemented trusted compiled-regex receiver tracking in `src/python/python-provenance.ts`, `src/python/python-known-methods.ts`, `src/python/python-inference.ts`, and `src/python/python-classifier.ts` with matching analyzer/runtime tests and reference-doc updates.
- 2026-03-20T18:02Z [CODE] [plan:77-python-module-family-review-batching] Extended `src/python/python-inference.ts` to carry tracked string split calls (`split`/`rsplit`/`splitlines`) through subscripted receivers and assigned locals, seed `collections.defaultdict(set|list)` and homogeneous dict-of-list/set locals, and clear those dict-value promotions on later subscript writes in `src/python/python-analyze.ts`. Added analyzer/runtime regressions plus extra negative runtime parity for `pytest.main`, importlib alias forms, Counter constructor capture, string-local reassignment, and dict-splat container locals.
- 2026-03-21T20:55Z [CODE] [plan:77-python-module-family-review-batching] Added helper-body scope seeding metadata through `src/python/python-timeline.ts` and `src/python/python-analyze.ts`, plus `dir` / `sorted` string-origin inference in `src/python/python-inference.ts`. Also tightened alias-aware invalidation for indexed and mutating list writes in `src/python/python-analyze.ts`, then added analyzer/runtime regressions for mixed helper callsites, shadowed builtins, indexed mutation, alias mutation, and comprehension mutation.
- 2026-03-23T14:24Z [CODE] [plan:77-python-module-family-review-batching] Added importlib metadata module-binding trust through `src/python/python-analyze.ts`, `src/python/python-scope.ts`, and `src/python/python-classifier.ts`; added exact `__dict__.items()` handling in `src/python/python-classifier.ts`; tightened helper/dir provenance by preserving alias links for tracked-container rebinding in `src/python/python-analyze.ts`; and added analyzer/runtime regressions for helper-def-before-producer, aliased/namespace importlib metadata, exact `__dict__.items()`, and alias/index/comprehension invalidation paths.
- 2026-03-23T17:50Z [CODE] [plan:73-python-analyzer-modularization] 2026-03-23T17:50Z Added follow-on Phase 10/11 work to plan 73 and completed both: extracted replay/import/assignment/helper-seed logic into `src/python/python-replay.ts`, then split inference into `python-inference-values.ts`, `python-inference-effects.ts`, `python-inference-paths.ts`, and `python-inference-containers.ts` with updated docs/install manifests.
- 2026-03-23T19:25Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-23T19:25Z Extended `src/python/python-replay.ts`, `src/python/python-timeline.ts`, `src/python/python-inference-containers.ts`, and the focused analyzer/runtime tests so helper-param seeding now covers direct lambda assignments plus trusted mutation-built string-list builders. The follow-up also added alias/base-mutation escape coverage and refreshed `docs/python-classification-reference.md` and plan 77 notes.
- 2026-03-23T20:23Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-23T19:25Z Added scope-aware helper exact-path storage in `src/python/python-replay.ts`, extra shadowing/rebinding/nested-builder regressions in the focused analyzer/runtime suites, and narrowed `docs/python-classification-reference.md` so direct dict-subscript reads are documented as conservative again.
- 2026-03-24T12:11Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T12:11Z Extended `src/python/python-replay.ts`, `src/python/python-provenance.ts`, and `src/python/python-analyze.ts` so direct same-syntax dict-subscript reads can reuse exact-path proofs in the main pass, then tightened invalidation for non-helper escapes, shadowing, alias-root rebinding, sibling/alternate-key mutations, and raw subscript-to-identifier escapes. Added matching analyzer/runtime regressions and refreshed the classification reference and plan notes.
- 2026-03-24T13:54Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T13:53Z Added `src/python/python-key-equivalence.ts`, wired exact-key lookup into `src/python/python-provenance.ts`, and extended `src/python/python-replay.ts` to persist and invalidate normalized exact-key proofs alongside raw receiver paths. Focused analyzer/runtime suites now cover value-equivalent dynamic-to-literal reads, literal-to-dynamic reads, preserved different-key siblings, owner-scope alias shadowing, and raw subscript escape invalidation.
- 2026-03-24T14:20Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:20Z Added exact string-set propagation in `src/python/python-key-equivalence.ts`, new scope storage in `src/python/python-analyze-types.ts` / `src/python/python-scope.ts`, replay persistence/invalidation in `src/python/python-replay.ts`, and matching provenance lookup in `src/python/python-provenance.ts`. Focused analyzer/runtime regressions now cover exact split/strip keys, iterated exact literal lines, and the conservative mixed-line boundary.
- 2026-03-24T15:25Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:27Z Extended `src/python/python-timeline.ts` to carry active guard conditions on timeline entries and taught `src/python/python-key-equivalence.ts` to narrow exact string sets through simple `startswith` guards. Added matching analyzer/runtime regressions for mixed literal-line loops and dynamic mixed-line negatives.
- 2026-03-24T15:39Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:40Z Extended `src/python/python-inference-paths.ts` so `iteratedPathValue()` now passes through trusted `sorted(...)` wrappers around identity generator expressions. Added focused analyzer/runtime regressions for `text.count` / `text.endswith` via `for path in sorted(p for p in root.glob('*.ts') if p.is_file())`, plus negatives for shadowed `sorted` and non-identity generators.
- 2026-03-24T16:41Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T15:05Z Landed the centralized trust-policy refactor across `python-known-methods.ts`, `python-scope.ts`, `python-classifier.ts`, and `python-inference-containers.ts`, then used it to clear `zlib.decompress`, bytes slice/split decode chains, and parent-derived path joins in `python-inference-containers.ts` / `python-inference-paths.ts` with matching analyzer/runtime regressions.
- 2026-03-24T17:54Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T17:05Z Landed the next exact-call family batch on top of the centralized trust registry: added guarded direct-import handling for `RequestInformation()`, guarded exact-direct-import purity for `tabulate.tabulate()` when `tablefmt` is absent or literal, and conservative `exec` support for trusted `mypy.api.run()` module bindings/direct imports, with analyzer/runtime regressions and classification-reference updates.

## [DISCOVERIES]
- 2026-03-24T13:54Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T13:53Z Focused verification passed for `cd .opencode && bun test test/python-analyze.test.ts` (223 pass), `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (77 pass), and `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (51 pass). A final read-only review approved the exact-key boundary. `--review-next --family e.split` / `e.startswith` still show the single session-report exemplar, and the refreshed family totals are now 44 snippets / 106 pending candidates / 79 families.
- 2026-03-24T14:18Z [TOOL] [plan:77-python-module-family-review-batching] Final read-only review of the latest exact string-value propagation confirms the new exact-value layer stays bounded to singleton literal proofs plus split/rsplit/splitlines and strip-family derivations on already exact receivers; mixed iterated literal lines still stay conservative without branch narrowing.
- 2026-03-24T15:25Z [TOOL] Compaction triggered: exceeded upper token threshold; truncated oldest entries to at or below lower target; archived to docs/MEMORY.md.
- 2026-03-24T14:20Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T14:20Z Focused verification passed for `cd .opencode && bun test test/python-analyze.test.ts` (226 pass), `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (80 pass), and `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (51 pass). Final review approved the bounded derivation layer. `--review-next --family e.split` / `e.startswith` still show the same session-report exemplar, confirming the remaining blocker is branch-sensitive or dynamic-origin exact values rather than direct key equivalence or pure-derivation support.
- 2026-03-24T15:25Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T14:27Z Focused verification passed for `cd .opencode && bun test test/python-analyze.test.ts` (227 pass), `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (81 pass), and `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (51 pass). `--review-next --family e.split` / `e.startswith` still show the same file-backed session exemplar, confirming the remaining blocker is dynamic-origin exact data rather than missing branch narrowing.
- 2026-03-24T15:39Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T14:40Z Focused verification passed for `cd .opencode && bun test test/python-analyze.test.ts` (229 pass), `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (53 pass), and `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (81 pass). `--review-next --family text.count` and `--family text.endswith` now report no pending review items, and the refreshed family totals dropped to 41 snippets / 90 pending candidates / 70 families.
- 2026-03-24T16:14Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T16:05Z Read-only review of post-`zlib.decompress` blocked families shows the next local gap is bytes-derived receiver provenance, not more string exact-value work: `data[:nul].decode()` loses type because `containerKind()` only preserves sliced `string`, and `raw.split(...)[1].decode()` loses type because `iteratedContainerKind()` only seeds split-element kind for tracked `string` receivers.
- 2026-03-24T16:41Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T15:05Z Focused verification passed for `cd .opencode && bun test test/python-analyze.test.ts` (237 pass), `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (83 pass), and `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (56 pass). `--review-next` now reports no pending items for `zlib.decompress`, `data.decode`, `raw.split.decode`, `raw.split.decode.splitlines`, `target.relative_to`, and `target.with_suffix`, and the refreshed family totals dropped to 36 snippets / 62 pending candidates / 55 families.
- 2026-03-24T16:53Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T17:05Z Read-only review of the next `RequestInformation` family found the blocker is canonicalization/trust, not `dir()` itself: `from kiota_abstractions.request_information import RequestInformation` stays bare because `shouldBindDirectImport()` only resolves selected targets, so the family lacks a provenance-backed module root. The representative snippet is a zero-arg constructor used only inside `dir(RequestInformation())`, which suggests a bounded exact-import pure-constructor rule is safer than generic constructor or introspection heuristics.
- 2026-03-24T17:23Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T18:10Z Read-only review of `mypy.api.run` shows the representative `from mypy import api; api.run(args)` path is mechanically expressible via the new trust registry, but unlike `RequestInformation`/`tabulate` it is not a pure bucket: official mypy docs/source say `api.run()` is the normal CLI in-process, reading files/config and potentially writing `.mypy_cache`.
- 2026-03-24T17:54Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T17:05Z Focused verification passed for `cd .opencode && bun test test/python-analyze.test.ts` (243 pass), `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (89 pass), and `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (56 pass). `--review-next` now reports no pending items for `RequestInformation`, `tabulate`, or `api.run`, and the refreshed family totals are down to 31 snippets / 57 pending candidates / 52 families.

## [OUTCOMES]
- 2026-03-23T20:53Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-23T21:05Z Final read-only review verdict is Needs Fixes. Exact same-syntax direct dict-subscript reads and documented path-mismatch conservatism look correct, but shadowed-key seeding and canonical-root alias invalidation still leave unsound direct-read proofs without dedicated regressions.
- 2026-03-24T11:41Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T11:41Z Review verdict remains Needs Fixes. Shadowing and alias-root rebinding coverage look correct in the reviewed scope, but direct exact-path dict-read safety is still unsound until same-root alternate-key mutations are either invalidated conservatively or proven equivalent with dedicated regressions.
- 2026-03-24T12:00Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T12:05Z Review verdict remains Needs Fixes. The remaining `inv[current]` -> literal-key family snippet is still only a conservative false negative, but exact same-syntax direct dict-subscript safety is not fully closed until raw root-aliased subscript escapes invalidate persisted receiver-element proofs and gain focused analyzer/runtime regressions.
- 2026-03-24T12:11Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T12:11Z Same-syntax direct dict-subscript reads now classify `e.split` / `e.startswith` as pure when they reuse the persisted exact builder path, while path-mismatched reads remain intentionally blocked. The next reduction in this bucket requires value-level key equivalence, not more root-scoped or alias-broad exact-path reuse.
- 2026-03-24T13:52Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T13:05Z Final read-only review of the exact-key equivalence follow-up found no new blocker-level issues in `src/python/python-key-equivalence.ts`, `src/python/python-provenance.ts`, `src/python/python-replay.ts`, the focused analyzer/runtime regressions, or `docs/python-classification-reference.md`. Verdict: Approve for the bounded exact-value key-equivalence layer and current invalidation surface; the remaining blocked snippet still depends on future exact derived-key value propagation.
- 2026-03-24T13:54Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T13:53Z Same-root value-equivalent reads are now allowed when the dict key is an exact proven string, so the remaining `e.split` / `e.startswith` blocker is no longer direct subscript equivalence. The next reduction in this bucket requires exact string-value propagation through bounded pure derivations and iterated literal lines.
- 2026-03-24T14:18Z [TOOL] [plan:77-python-module-family-review-batching] Verdict: Approve. The current invalidation surface for exact-key and derived exact-string proofs looks sound in the reviewed scope, and the remaining session-report `e.split`/`e.startswith` blocker still comes from branch-sensitive or dynamic-origin exact values, not a new safety hole.
- 2026-03-24T14:20Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:20Z Exact key equivalence now covers bounded pure string derivations and exact literal-line iteration, but the saved-session queue still has one `e.split` / `e.startswith` exemplar because `inv_lines` comes from file-read content and mixed loop branches. The next safe reduction is control-flow/value narrowing for exact string sets, not wider dict-root reuse.
- 2026-03-24T15:25Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:27Z The safe `e.split` / `e.startswith` reductions inside plan 77 are effectively exhausted: exact-path proof, exact-key equivalence, pure-derivation exact values, iterated literal lines, and simple branch narrowing are now all covered. The surviving exemplar depends on dynamic file content, so further progress in this bucket would require a larger content-sensitive/static-data design rather than another small provenance patch.
- 2026-03-24T15:39Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T14:40Z The analyzer now retains `Path` provenance through trusted sorted identity generators, which clears the blocked `text.count` / `text.endswith` family without relaxing string receiver safety. With the current string buckets exhausted, the next likely family to attack is `zlib.decompress`.
- 2026-03-24T16:14Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T16:05Z Recommended next safe patch is bounded producer-side bytes provenance in `python-inference-containers.ts`: preserve slice kinds for tracked `bytes`/`bytearray`/`memoryview` and seed element kinds for `bytes`/`bytearray` `split`/`rsplit`/`splitlines`. Keep interprocedural helper return tuples (for example `read_loose()` -> `body`) out of scope for this patch.
- 2026-03-24T16:31Z [TOOL] [plan:77-python-module-family-review-batching] Read-only review of scalable trust-policy gating plus zlib/bytes provenance approves the current plan-77 scope: `trustedCallWithPolicy()` now gates both classifier and temporary-container inference, and the new bytes slice/split provenance stays bounded to already tracked receivers. Minor follow-ups only: duplicated zlib analyzer tests and missing direct regressions for the generalized `bytearray`/`memoryview` producer branches.
- 2026-03-24T16:41Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T15:05Z The safe producer-side reductions around the zlib/git-object and path-parent clusters are now exhausted. Remaining `e.split`/`e.startswith` and `body.decode`/`text.splitlines` exemplars require larger dynamic/interprocedural designs, so the next small bucket should come from a new family such as `RequestInformation` or `tabulate` rather than more local provenance tweaks in these clusters.
- 2026-03-24T17:23Z [TOOL] [plan:77-python-module-family-review-batching] 2026-03-24T18:10Z Recommendation: do not treat `mypy.api.run` as the next low-risk pure/read exact-call bucket after `RequestInformation` and `tabulate`; if pursued, scope it explicitly as an `exec` exact-call family with `module-binding-or-direct-import` trust and conservative alias/launder negatives.
- 2026-03-24T17:54Z [CODE] [plan:77-python-module-family-review-batching] 2026-03-24T17:05Z The remaining queue is no longer rich in one-file exact-call wins; the recent exact-call and producer-side batches appear locally exhausted. What remains at the top is the already-explored dynamic string/interprocedural provenance cluster (`e.split`, `e.startswith`, `text.splitlines`, `body.decode`) plus medium-size container/value-provenance work, so future reductions are less likely to be small registry/rule patches.
- 2026-03-24T17:59Z [TOOL] [plan:77-python-module-family-review-batching] Final read-only review of the post-policy-name exact-call batch found no blocking alignment issues: `TRUSTED_CALL_POLICIES` / `trustedCallWithPolicy()` now enforce the intended trust split for `RequestInformation`, `tabulate`, and `mypy.api.run`, the new guard primitives stay bounded to literal/arity/`**kwargs` checks, and analyzer/runtime/docs coverage matches the intended direct-import vs module-binding boundaries. Only minor non-blocking follow-up test opportunities remain (for example `**kwargs` negatives and aliased `mypy` module-binding runtime coverage).
