# CONTINUITY

## [PLANS]
- 2026-03-10T18:54Z [USER] [plan:72-python-review-identity-and-promotion-safety] Requested a saved implementation plan for review-identity ledger/reuse/promotion changes in `src/python-session-report.ts` based on a patch-ready proposal that replaces bare call-level reuse with `kind` plus canonical callable context.
- 2026-03-13T00:52Z [USER] [plan:73-python-analyzer-modularization] User requested writing a new implementation plan file for breaking up `src/python/python-analyze.ts` into smaller modules.
- 2026-03-17T13:08Z [USER] [plan:76-python-permission-provenance-view] Requested a read-only, commit-safe implementation plan for adding provenance to the Python permission preview/allow view across `src/python.ts`, TUI permission helper/renderer, focused runtime/TUI tests, and concise doc touchpoints.
- 2026-03-17T13:11Z [USER] [plan:76-python-permission-preview-provenance] Requested writing a new implementation plan for showing compact provenance context in the Python permission preview/allow view.

## [DECISIONS]
- 2026-03-13T01:35Z [CODE] [plan:73-python-analyzer-modularization] Phase 1 treats `empty-source` as analyzer-only runtime-unreachable input validation behavior, while preserving the existing `no-classified-call` fallback branch as documented-but-unreproduced from the public analyzer API.
- 2026-03-13T10:31Z [CODE] [plan:73-python-analyzer-modularization] Phase 2 keeps `PythonAnalyzeResult` in `src/python/python-ir.ts` to avoid introducing a type cycle, while Phase 4 keeps `python-events.ts` dependent only on outward contract and IR/value seams rather than analyzer state.
- 2026-03-13T14:06Z [CODE] [plan:73-python-analyzer-modularization] Phase 7 keeps replay sequencing in `src/python/python-analyze.ts` but moves parsing, inference, response-json, and builtin-pure support helpers into `python-inference.ts`; Phase 8 moves `classifyRaise()` with the classifier so raise/call effect resolution shares one seam.
- 2026-03-14T02:19Z [USER] Repo-local `python-review-pipeline` skill should not ask the user for intermediate candidate decisions; it must drive the review queue to completion autonomously unless a genuinely unresolvable ambiguity or unsafe/destructive action is encountered.
- 2026-03-16T16:37Z [USER] Workspace-read parity should auto-allow only statically resolved literal filesystem reads inside the active worktree; dynamic reads must remain permission-gated.
- 2026-03-16T20:49Z [TOOL] [plan:75-python-runtime-test-modularization] collectInlinePermissionParity stays in python.test.ts through Phase 1-3 and moves with its sole consumer (parity smoke fixtures) in Phase 4, per the keep-local-unless-two-files-need-it constraint. always-pattern-checks belong in python-external-directory.test.ts because they exercise permission shapes specific to script-file/external-directory contexts.
- 2026-03-17T13:18Z [CODE] [plan:76-python-permission-preview-provenance] Canonical slug for the saved plan is `76-python-permission-preview-provenance`; the earlier read-only exploration slug `76-python-permission-provenance-view` is superseded by this saved plan file.
- 2026-03-17T13:55Z [USER] [plan:76-python-permission-preview-provenance] Provenance preview rows should stay on one line in bracketed `[kind] [source] [provenance]` form; omit the third segment when no high-signal provenance context exists rather than wrapping.
- 2026-03-17T14:01Z [USER] [plan:76-python-permission-preview-provenance] The bracketed provenance row format should omit the third segment by default unless it adds meaningful signal beyond `[kind] [source]`; low-value repetitive context should stay hidden.

## [PROGRESS]
- 2026-03-12T18:32Z [CODE] Expanded `src/python/python-rules.json` guarded Path read/write coverage to the current public method surface, added exact/pure/path-returning handling for `Path.home`, `Path.from_uri`, and Path helper methods in `src/python/python-analyze.ts`, and taught `Path.open(...)` to classify against the receiver path with builtin-mode semantics.
- 2026-03-12T20:06Z [CODE] 2026-03-12T00:00Z Fixed comprehension rebinding order in `collectTimeline()` so `for_in_clause` targets sort before comprehension body calls, extended iterable element inference for homogeneous standard-container literals/comprehensions and `list`/`tuple`/`set` wrappers, and preserved iterated-element metadata even when an assignment also records the outer container kind.
- 2026-03-12T21:34Z [CODE] 2026-03-12T00:00Z Added broad `inspect.*` call coverage in `src/python/python-rules.json` (pure metadata helpers, read-oriented source/frame walkers, guarded pure rules for `inspect.signature`, `inspect.get_annotations`, and `inspect.Signature.from_callable` when annotation-eval kwargs are absent), extended direct-import binding for canonical `inspect.*` names, and tracked inspect receiver kinds for `Signature`, `Parameter`, and `BoundArguments` so methods like `.bind()`, `.replace()`, `.format()`, and `.apply_defaults()` classify as pure.
- 2026-03-13T00:52Z [CODE] [plan:73-python-analyzer-modularization] Added `docs/plans/73-python-analyzer-modularization.md` with scope, target module boundaries, parity constraints, eight commit-safe phases, rollout gates, and acceptance criteria for behavior-preserving analyzer refactoring.
- 2026-03-13T00:58Z [CODE] [plan:73-python-analyzer-modularization] 2026-03-13T01:15Z Updated `docs/plans/73-python-analyzer-modularization.md`: expanded from 8 to 9 phases, added module dependency direction section, detailed function-level extraction lists per phase, added cycle-detection validation steps. All phases remain TODO.
- 2026-03-13T01:01Z [CODE] [plan:73-python-analyzer-modularization] 2026-03-13T01:25Z Added end-state file hierarchy section to plan 73 showing the target 18-file layout with 7 new modules, approximate line distributions, and before/after metrics table.
- 2026-03-13T01:33Z [TOOL] [plan:73-python-analyzer-modularization] Completed follow-up read-only re-review for Phase 1 scope files (`.opencode/test/python-analyze.test.ts`, `.opencode/test/python.test.ts`, `docs/plans/73-python-analyzer-modularization.md`) with parity context from `src/python/python-analyze.ts`.
- 2026-03-13T01:35Z [CODE] [plan:73-python-analyzer-modularization] Completed Phase 1 parity hardening: added exact `analyzeDetailed()` fixture tables in `.opencode/test/python-analyze.test.ts` plus runtime permission/metadata smoke fixtures in `.opencode/test/python.test.ts`.
- 2026-03-13T10:31Z [CODE] [plan:73-python-analyzer-modularization] Completed Phases 2-4: extracted shared types/static tables into `python-analyze-types.ts` and `python-known-methods.ts`, moved loader/cache logic into `python-bootstrap.ts`, and moved event/result helpers into `python-events.ts`.
- 2026-03-13T14:06Z [CODE] [plan:73-python-analyzer-modularization] Completed Phases 5-9: extracted `python-scope.ts`, `python-timeline.ts`, `python-inference.ts`, and `python-classifier.ts`; trimmed `python-analyze.ts` to an orchestration-focused public entry; refreshed README and design docs to match the modular layout.
- 2026-03-13T17:20Z [CODE] Added receiver-path provenance alongside receiver container provenance so attribute/subscript-assigned `pathlib.Path` values can be recovered during method classification; `trackedPathValue()` now also preserves Path-ness for `/` joins with non-literal RHS by returning a dynamic path value instead of dropping provenance.
- 2026-03-13T17:35Z [CODE] Extended `trackedPathValue()` to recognize zero-arg local factory calls that return proven Path-valued expressions (identifier/attribute/call/binary `/` join) via `callableFactoryReturn()`, while keeping callable alias promotion limited to identifier/attribute returns only.
- 2026-03-13T18:37Z [CODE] Extended `python-inference.ts` with `unwrapParenthesized()` for path/bytes/string inference and bounded `bytes`/`bytearray` `decode(...) -> string` temporary receiver inference, so parenthesized Path joins now classify `read_bytes` correctly and decode chains can continue as tracked string methods.
- 2026-03-13T20:18Z [CODE] Added bounded `ast` module pure-call support by allowlisting explicit `ast.*` helpers in `src/python/python-rules.json` and extending direct-import canonicalization in `src/python/python-scope.ts`, so `import ast` and `from ast import parse` both resolve to canonical pure calls without broad method-name matching.
- 2026-03-14T02:11Z [CODE] Created a repo-local skill at `.claude/skills/python-review-pipeline/SKILL.md` that defines the saved-session review workflow as a queue-draining family-based procedure: inspect full snippets, classify constructors plus bounded receiver methods, patch analyzer/rules when recurring noise should disappear, and continue until `--review-next` reports no candidates.
- 2026-03-14T02:19Z [CODE] Updated `.claude/skills/python-review-pipeline/SKILL.md` to require autonomous queue draining with no per-candidate user prompts, explicit continuation between candidates, and stopping only for real blockers or an empty queue.
- 2026-03-14T03:10Z [CODE] Extended classifier support for `unittest.mock` by allowlisting `Mock`/`AsyncMock`/related constructors plus `patch*` helpers in `src/python/python-rules.json`, adding direct-import binding in `src/python/python-scope.ts`, and teaching `src/python/python-inference.ts` to track `mock` / `async-mock` receiver kinds so bounded assertion/reset/configuration methods classify as pure.
- 2026-03-16T16:37Z [CODE] Added `docs/plans/74-python-workspace-read-parity.md` and implemented Phase 1-3: `src/python.ts` now suppresses `python` permission asks for literal read paths while preserving read operations metadata, and outside-worktree `scriptPath` now triggers `external_directory` approval before the script file is read.
- 2026-03-16T19:03Z [CODE] Completed Phase 4 of `docs/plans/74-python-workspace-read-parity.md`: `src/python.ts` now uses component-walking canonical boundary checks plus fail-closed `external_directory` fallback for boundary-resolution failures, and `.opencode/test/python.test.ts` now covers symlink-inside, symlink-outside, symlink-plus-`..`, `ELOOP`, and `ENOTDIR` cases for literal reads, `scriptPath`, and `workdir`.
- 2026-03-16T19:14Z [USER] Synced repo runtime `src/python.ts` into global tool path `~/.config/opencode/tools/python.ts`, rewrote the plugin import to `@opencode-ai/plugin`, and created a backup of the prior global file.
- 2026-03-16T20:49Z [TOOL] [plan:75-python-runtime-test-modularization] Applied 5 review fixes to plan 75: (1) clarified collectInlinePermissionParity deferred to Phase 4, (2) made workdir semantics extraction explicit with test counts in Phase 2, (3) added concrete pre-split baseline inventory capture step with 116-test count target, (4) added coupling rationale for always-pattern-checks in Phase 3, (5) added .claude/skills/python-review-pipeline/SKILL.md to Phase 6 scope. Also added plan_type: build to frontmatter.
- 2026-03-17T12:13Z [CODE] [plan:75-python-runtime-test-modularization] Completed Phase 1 by extracting shared runtime test helpers/constants from `.opencode/test/python.test.ts` into new fixture module `.opencode/test/fixtures/python-runtime.ts` while keeping `collectInlinePermissionParity()` local to the monolith for later Phase 4 movement.
- 2026-03-17T12:52Z [CODE] [plan:75-python-runtime-test-modularization] Completed Phase 2 by moving the `arg validation`, `subprocess hard-fail`, and `ENOENT handling` suites into `.opencode/test/python-validation.test.ts` and the `workdir semantics` plus gated `process execution, timeout, abort, and streaming` suites into `.opencode/test/python-runtime-execution.test.ts`, leaving `.opencode/test/python.test.ts` as the remaining permission/boundary host.
- 2026-03-17T13:08Z [CODE] [plan:75-python-runtime-test-modularization] Completed Phase 3 by moving the `script-file permission behavior`, `external directory detection`, and `always pattern checks` suites into `.opencode/test/python-external-directory.test.ts`, leaving `.opencode/test/python.test.ts` with only inline permission-planning coverage ahead of Phase 4.
- 2026-03-17T13:15Z [CODE] [plan:75-python-runtime-test-modularization] Resolved the low-risk Phase 3 review follow-ups by broadening `.opencode/test/fixtures/python-runtime.ts` metadata typing to include `analyzerVersion` and `engineVersion`, and by clarifying in the plan that `always pattern checks` are colocated with the boundary host file for permission-shape cohesion rather than because they are boundary-only cases.
- 2026-03-17T13:18Z [TOOL] [plan:76-python-permission-preview-provenance] Quality review tightened the saved plan by requiring visible directory/pattern context for Python-shaped `external_directory` prompts, explicit compact/fullscreen provenance row caps (4/12), stable row ordering, malformed-row dropping, and Phase 3 manual renderer verification.
- 2026-03-17T13:25Z [CODE] [plan:75-python-runtime-test-modularization] Completed Phase 4 by moving 20 low-coupling inline permission tests into `.opencode/test/python-inline-permissions-basic.test.ts` and relocating `collectInlinePermissionParity()` with the parity smoke fixtures, leaving `.opencode/test/python.test.ts` with only the 51 inference-heavy inline suites for Phase 5.
- 2026-03-17T13:37Z [TOOL] [plan:76-python-permission-preview-provenance] Applied 3 review fixes to plan 76: (1) added `.opencode/test/python-external-directory.test.ts` to In Scope and Phase 1 file list with targeted provenance-parity test bullets, (2) added explicit Phase 3 bullet to wire the dead-code `patterns` parameter in `renderPythonBody()` for external_directory prompts, (3) clarified no-dedup rule as first-pass simplification noting analyzer may emit multiple entries per call site.
- 2026-03-17T13:55Z [TOOL] [plan:76-python-permission-preview-provenance] Updated the plan to require single-line bracketed provenance rows, wired that contract into the Phase 2 view-model bullet, the Phase 3 renderer bullet, and the acceptance criteria.
- 2026-03-17T13:57Z [CODE] [plan:75-python-runtime-test-modularization] Completed Phase 5 by moving the remaining 51 inference-heavy inline suites into `.opencode/test/python-inline-permissions-inference.test.ts` and deleting `.opencode/test/python.test.ts`, leaving the runtime suite fully split across five focused test files ahead of Phase 6 doc updates.
- 2026-03-17T14:01Z [TOOL] [plan:76-python-permission-preview-provenance] Refined the row-format contract so the third bracket is optional-by-default and only shown when it adds meaningful signal; mirrored that rule in the acceptance criteria.
- 2026-03-17T14:53Z [CODE] [plan:75-python-runtime-test-modularization] Completed Phase 6 by updating `README.md`, `docs/python-custom-tool-plan.md`, `.claude/skills/python-review-pipeline/SKILL.md`, and the active plan to describe the split runtime suite, shared fixture, historical monolith note, and final done status for plan 75.
- 2026-03-17T15:13Z [CODE] [plan:76-python-permission-preview-provenance] Refreshed `docs/plans/76-python-permission-preview-provenance.md` after plan 75 completed by removing stale `.opencode/test/python.test.ts` references, keeping Phase 1/4 runtime validation targeted to `.opencode/test/python-external-directory.test.ts`, and noting that existing `python`-ask provenance coverage now lives in the split inline runtime tests.

## [DISCOVERIES]
- 2026-03-17T12:18Z [TOOL] [plan:75-python-runtime-test-modularization] Read-only Phase 1 re-review of `.opencode/test/python.test.ts`, `.opencode/test/fixtures/python-runtime.ts`, and `docs/plans/75-python-runtime-test-modularization.md` found no blocking semantic drift in the extracted helpers, but the new fixture exports undocumented types (`MockContext`, `PythonAskMetadata`) and `getAskMetadata()` relies on a hand-maintained cast-based metadata shape that can drift from the real ask contract as later split files adopt it.
- 2026-03-17T12:37Z [TOOL] [plan:75-python-runtime-test-modularization] 2026-03-17T00:00Z read-only Phase 2 review found numeric parity preserved after the split: `.opencode/test/python.test.ts` now has 99 tests, `.opencode/test/python-validation.test.ts` has 8, and `.opencode/test/python-runtime-execution.test.ts` has 9, matching the Phase 2 baseline target of 116 in the plan.
- 2026-03-17T12:55Z [TOOL] [plan:75-python-runtime-test-modularization] 2026-03-17T13:10Z final Phase 2 re-review found count/title parity preserved after the validation/execution split (99 + 8 + 9 = 116) and no moved-suite duplication left in `.opencode/test/python.test.ts`, but `.opencode/test/fixtures/python-runtime.ts` still hand-maintains `PythonAskMetadata` without `analyzerVersion`/`engineVersion`, so the helper type has already drifted from current monolith assertions.
- 2026-03-17T13:08Z [TOOL] [plan:76-python-permission-provenance-view] Current runtime already attaches per-operation provenance (`canonicalSource`, `receiverKind`, `dependencySignature`, `ruleHit`, `guardFailure`) inside `metadata.operations` for `python` asks, but `permission-python.ts` ignores `operations` entirely and `external_directory` asks in `src/python.ts` carry only preview/source fields, not permissionMetadata.
- 2026-03-17T13:22Z [TOOL] [plan:75-python-runtime-test-modularization] 2026-03-17T13:25Z Phase 4 read-only review of `.opencode/test/python.test.ts` found the safest basic/inference split boundary as 20 basic tests (preview/ask-shape/no-ask + 5 parity fixtures + late emit/write and pure/unknown mixed cases) versus 51 inference-heavy tests (Path/string/builtin/container/json/response/self/HTTP/conservative guardrails). `collectInlinePermissionParity()` is only used by the 5 parity fixtures, so it should move intact into `python-inline-permissions-basic.test.ts` rather than into `fixtures/python-runtime.ts`.
- 2026-03-17T13:22Z [TOOL] Compaction triggered: exceeded upper token threshold; truncated oldest entries to at or below lower target; archived to docs/MEMORY.md.
- 2026-03-17T15:12Z [TOOL] [plan:76-python-permission-preview-provenance] 2026-03-17T15:xxZ read-only reflection confirmed `docs/plans/76-python-permission-preview-provenance.md` now describes split inline runtime coverage instead of the retired `.opencode/test/python.test.ts` monolith; exact-path grep for `.opencode/test/python.test.ts` in that file returns no matches, while loose `python\.test\.ts` regexes falsely match `permission-python.test.ts`.

## [OUTCOMES]
- 2026-03-17T12:18Z [TOOL] [plan:75-python-runtime-test-modularization] Phase 1 helper extraction review verdict: needs follow-up, not fixes. Suggested narrowing or documenting the exported fixture API, deriving ask metadata types from the real context contract, and aligning the formal Phase 1 validation block with the suite-level `bun test` run already recorded in the plan notes.
- 2026-03-17T12:37Z [TOOL] [plan:75-python-runtime-test-modularization] 2026-03-17T00:00Z Phase 2 review verdict: Needs Follow-up. No blocking semantic drift or import/helper breakage found in the moved validation/execution suites, but `docs/plans/75-python-runtime-test-modularization.md` still marks Phase 2 TODO and does not record the required baseline title inventory or validation notes; low-risk follow-up is reusing `INVALID_PYTHON_BIN` in the ENOENT test to avoid helper drift.
- 2026-03-17T12:52Z [TOOL] [plan:75-python-runtime-test-modularization] Validated Phase 2 with `cd .opencode && bun test test/python-validation.test.ts` (8 pass), `cd .opencode && bun test test/python-runtime-execution.test.ts` (9 pass), `cd .opencode && bun test test/python.test.ts` (99 pass), and `cd .opencode && bun test` (348 pass); numeric parity matches the pre-split 116-test runtime baseline as 99 + 8 + 9.
- 2026-03-17T12:55Z [TOOL] [plan:75-python-runtime-test-modularization] 2026-03-17T13:10Z final Phase 2 re-review verdict: Needs Follow-up. Validation/execution suites and Phase 2 notes now align on moved titles/counts plus `INVALID_PYTHON_BIN` reuse, but the plan file still does not preserve the full promised 116-title baseline inventory needed as later-phase audit source of truth.
- 2026-03-17T13:08Z [TOOL] [plan:75-python-runtime-test-modularization] Validated Phase 3 with `cd .opencode && bun test test/python-external-directory.test.ts` (28 pass), `cd .opencode && bun test test/python.test.ts` (71 pass), and `cd .opencode && bun test` (348 pass); the 99-test pre-split monolith remainder now reconciles as 71 + 28.
- 2026-03-17T13:11Z [CODE] [plan:76-python-permission-preview-provenance] Added `docs/plans/76-python-permission-preview-provenance.md` with commit-safe phases covering runtime metadata parity, TUI helper normalization, renderer updates, and docs/manual validation for Python permission provenance previews.
- 2026-03-17T13:14Z [TOOL] [plan:75-python-runtime-test-modularization] Read-only Phase 3 review verdict: Needs Follow-up. Count/title parity holds after the boundary split (`python.test.ts` 71 + `python-external-directory.test.ts` 28 = prior 99) and the 28 moved titles exactly match the Phase 2 inventory, but Phase 3 plan notes overstate `always pattern checks` as boundary/context.ask-coupled even though those tests are generic inline permission checks, and `fixtures/python-runtime.ts` still omits `analyzerVersion`/`engineVersion` from `PythonAskMetadata` despite monolith assertions using them.
- 2026-03-17T13:22Z [TOOL] [plan:75-python-runtime-test-modularization] 2026-03-17T13:25Z Produced a read-only Phase 4 implementation recommendation: create `python-inline-permissions-basic.test.ts` first, move the 20 low-coupling tests with the local parity helper, leave 51 inference-heavy tests in `python.test.ts`, then validate file count parity and preserve all titles before Phase 5 renames/removes the monolith.
- 2026-03-17T13:25Z [TOOL] [plan:75-python-runtime-test-modularization] Validated Phase 4 with `cd .opencode && bun test test/python-inline-permissions-basic.test.ts` (20 pass), `cd .opencode && bun test test/python.test.ts` (51 pass), and `cd .opencode && bun test` (348 pass); the 71-test pre-split inline monolith remainder now reconciles as 20 + 51.
- 2026-03-17T13:57Z [TOOL] [plan:75-python-runtime-test-modularization] Validated Phase 5 with `cd .opencode && bun test test/python-inline-permissions-inference.test.ts` (51 pass) and `cd .opencode && bun test` (348 pass); pre/post move parity is exact because the retired `.opencode/test/python.test.ts` and new inference file share the same 51-test count and SHA-256 `75ca1abbca676fcc6ff68a049a8213221edff09bf88c752efd177f903782c63d`, and the five split runtime files total the baseline 116 tests (8 + 9 + 28 + 20 + 51).
- 2026-03-17T14:53Z [TOOL] [plan:75-python-runtime-test-modularization] Validated Phase 6 with `cd .opencode && bun test` (348 pass). Targeted grep showed no active `python.test.ts` references remain in `README.md` or `.claude/skills/python-review-pipeline/SKILL.md`; remaining hits are intentional historical notes in `docs/python-custom-tool-plan.md` plus follow-up cleanup in `docs/plans/76-python-permission-preview-provenance.md`.
- 2026-03-17T15:12Z [TOOL] [plan:76-python-permission-preview-provenance] 2026-03-17T15:xxZ captured concise cleanup reflection for the post-modularization doc touch-up: dependent plan wording is now aligned with the split runtime suite and validation should use anchored grep patterns for stale-reference checks.
- 2026-03-17T15:13Z [TOOL] [plan:76-python-permission-preview-provenance] 2026-03-17T15:00Z read-only review of the plan-76 stale-test-reference cleanup returned Approve: `docs/plans/76-python-permission-preview-provenance.md` now names the split inline runtime files instead of retired `.opencode/test/python.test.ts`, which matches plan 75’s final five-file layout. Optional doc nit: bump plan 76 `updated` if this lands as a separate edit.
- 2026-03-17T15:13Z [TOOL] [plan:76-python-permission-preview-provenance] Exact-path stale-reference validation for `docs/plans/76-python-permission-preview-provenance.md` found no remaining `.opencode/test/python.test.ts` hits; remaining grep matches were only `permission-python.test.ts` paths in the TUI package.
